# 动态调整机制研究计划

## 研究对象

Neuro Engine 不再以“补齐一个功能完备的神经元基座”为近期目标。节点只作为最小动态载体，承诺有限状态、输入输出、局部连接、资源和可观测性；项目真正比较的是：**什么样的受约束调整机制，能让同一载体形成、保留和迁移宏观能力。**

可以把系统写成两层：

```text
运行：      x(t+1) = F(x(t), input(t); θ(t))
自我调整：  θ(t+1) = G(θ(t), local history, consequence, resource)
```

`F` 是当前系统怎样运行，`G` 是系统怎样改变自己。节点模型、连续状态或 LIF 都可以替换；研究主变量是 `G` 及其与稳定机制的组合。

## 调整机制契约

候选机制必须满足：

- 在线运行，不依赖完整训练集或人工分段重置；
- 主要读取局部状态、局部连接和局部资格历史；
- 全局信号只能表示后果或调制，不能直接写入正确动作；
- 活动、资源、权重和新增内部变量均有界；
- 不为每个任务重新设计规则或重新选择参数；
- 具有冻结、关闭、置乱或预算匹配对照；
- 在独立系统种子和未见任务条件上报告形成概率；
- 外部读出器不能替主体承担能力。

调整机制可以处于不同时间尺度：

| 层次 | 调整对象 | 当前状态 |
|---|---|---|
| 快速状态 | 活动、短期记忆、动作分布 | 已有 Gate B～E 证据 |
| 中速可塑性 | 感觉和循环权重 | Gate F、Map 2A 已实现 |
| 慢速调制 | 可塑率、稳定度、局部资源分配 | 仅有统一资源调制 |
| 结构调整 | 固定预算来源槽位重分配 | M2C 首个候选已淘汰 |
| 元调整 | 调整规则自身随历史改变 | 尚未实现 |

## 能力向量

后续不以单一最高准确率选择机制。每个机制同时报告：

- `adaptation`：第一次学习或规则变化后的适应速度；
- `retention`：学习新规则后旧规则保留多少；
- `reacquisition`：旧规则重现时是否明显快于第一次学习；
- `transfer`：未见输入编码、时序或任务是否受益；
- `stability`：活动、资源和参数是否长期有界；
- `robustness`：损伤、噪声和持续输入下是否保持功能；
- `efficiency`：为形成能力消耗的事件、更新和状态预算。

能力形成仍要求行为、学习、泛化、跨种子、因果和持续性证据同时成立。不同微观结构不必相同，但宏观能力向量和干预方向必须可重复。

## 阶段 M0：冻结参考载体与机制接口

> 状态：已完成。正式记录见 [experiments/mechanism-m0-v0.1.md](experiments/mechanism-m0-v0.1.md)，机器可读清单为 `app/public/mechanism-m0-v0.1.json`。

### 目标

把 Map 2 的 24 单元系统冻结为 `reference-substrate/v1`。它是机制实验载体，不是完备神经元模型。

### 工作

- 明确所有可调整变量：活动状态、感觉权重、循环权重、兴奋性、资源及后续机制状态；
- 为每类变量定义读取范围、更新权限、预算和冻结方式；
- 将当前软边界可塑性、持续资源和统一调制封装为参考调整机制；
- 固定开发/确认种子、任务生成规则和对照接口；
- Web 区分“载体状态”“调整动作”和“能力结果”。

### 完成条件

参考结果完全回归 Map 2，关闭任何新接口时不改变正式哈希；候选机制能够只通过接口注入，不能绕过任务闭环直接修改输出。

### 已冻结实现

- `AdjustmentMechanism` 只接收活动、局部连接、资格、后果和资源观测；
- 动作类型只允许写活动、循环权重、兴奋性和资源，感觉权重在参考载体中在线冻结；
- 目标标签、动作概率、读出权重和任务生成器不出现在机制接口；
- 接口边界拒绝非有限写入并执行声明范围；
- Map 2A/B/C 完整序列化结果逐字段回归三个发布 JSON，原 SHA-256 保持不变。

## 阶段 M1：多规则保留边界

> 状态：已完成。协议通过但保留边界未通过；主导失败为容量不足。正式记录见 [experiments/mechanism-m1-v0.2.md](experiments/mechanism-m1-v0.2.md)。

### 问题

当前 Map 2C 只证明二元规则间持续适应，不能区分保留和反复重学。先不增加机制，直接测出现有调整规则的遗忘边界。

### 任务

- 使用固定的多符号输入和二元动作，构造至少四个不同映射规则；
- 采用 `A → B → C → D → A` 连续序列，不重置活动、资源或权重；
- 输入投影、动作读出和调整规则在规则切换前冻结；
- 同时保留冻结可塑性、随机后果、逐试次重置和单规则容量对照；
- 比较规则第一次学习、离开前表现、重现时初始表现和重新达到阈值所需试次。

### 输出

将失败明确归类为：容量不足、覆盖性遗忘、信用路由错误、资源耗尽或动力学失稳。M1 是诊断阶段；即使失败，只要协议完整也是有效结果。

### 冻结结果

- A 离开前 `83.4%`，回归初始 `60.7%`，表面下降 `22.7 pp`；
- B/C/D 连续学习后的平均准确率只有 `55.4%`；
- 四规则单独训练时的最低准确率平均只有 `51.3%`；
- 冻结可塑性和随机后果分别为 `45.9%`、`47.6%`，证明现有调整有贡献但容量不足；
- 六个参考参数点全部归类为容量不足，不触发 M2A 元可塑性；M2C 结构调整的进入条件成立。

## 阶段 M2：条件式机制修订

M2 不预先把所有生物机制加入系统，而根据 M1 的主要失败类型选择一个分支。一次正式实验只能改变一个机制。

### M2A：元可塑性——仅在覆盖性遗忘占主导时

为每条可塑连接增加一个慢速、局部且有界的可塑状态，使长期一致证据逐渐降低该连接被覆盖的速度，同时允许矛盾证据重新开放。比较：

- 当前固定可塑率；
- 元可塑性；
- 相同额外状态预算但随机或固定调制；
- 完全冻结旧连接。

通过条件不是只提高旧规则保留，而是在不超过冻结容差地损害新规则学习的前提下，提高保留和重现速度。

### M2B：局部调制——仅在信用路由错误占主导时

让全局后果只调制具有近期局部资格的连接群，而不是平均影响全部开放连接。必须保持全局后果携带的信息量不变，并用置乱资格归属和全局统一调制做配对对照。

### M2C：结构调整——仅在固定拓扑或容量成为明确瓶颈时

> 状态：已完成首个候选实验。协议通过，候选未通过机制验收；正式记录见 [experiments/mechanism-m2c-v0.3.md](experiments/mechanism-m2c-v0.3.md)。

允许连接在固定总预算下增加、删除或重新分配。结构改变只能读取局部活动、局部后果和资源；必须与等连接数随机重连、仅权重可塑和冻结结构比较。不能把目标规则或节点语义写入生长规则。

冻结实现保持每单元 6 条循环入边和 2 个可塑来源槽位，只按奖励调制的局部资格证据移动来源。开发阶段选择每 64 试次重连一次、证据衰减 `0.80`；独立确认中，局部重连的单规则最低准确率为 `47.9%`，仅权重可塑为 `48.7%`，B/C/D 连续学习分别为 `59.5%` 和 `59.6%`。局部规则也未可靠优于等次数随机重连。该候选归入 `NoBenefitOverWeightOnly`，不进入 M3。

离线结构证据诊断现已完成，正式记录见 [experiments/structural-diagnostic-v0.4.md](experiments/structural-diagnostic-v0.4.md)。在 48 个独立运行、全部 864 个重连检查点和 14688 个候选反事实中，oracle 单边换边平均只有 `+1.38 pp`，低于 `2 pp` 门槛；证据排序相关仅 `0.046`。首选相对随机虽有 `+0.20 pp`，但远低于 `1 pp` 实用门槛。A 回归阶段存在局部正信号，但不能替代总体结论。正式决策为 `CounterfactualEffectsFlat`。

M2C-T 离线时间尺度诊断也已完成，正式记录见 [experiments/structural-timescale-v0.5.md](experiments/structural-timescale-v0.5.md)。它逐项复现 v0.4 的 32 试次反事实，并沿同一累计轨迹比较 `[0, 32, 128, 256]`。oracle 收益分别为 `+0.62 / +1.38 / +1.40 / +1.24 pp`；32 → 256 的配对变化为 `-0.15 pp [-0.36, +0.07]`。长窗口没有揭示延迟结构收益，正式决策为 `SingleEdgeFreedomInsufficient`。

M2C-G 固定预算成组诊断也已完成，正式记录见 [experiments/structural-group-v0.6.md](experiments/structural-group-v0.6.md)。在每种规模相同的 17-bundle 搜索预算下，1/2/4 边 budgeted oracle 为 `+1.38 / +1.80 / +2.55 pp`；4 边相对单边为 `+1.16 pp [0.91, 1.42]`，并有 `+0.95 pp` 正组合交互。但已有 A 阶段 oracle 为 `+4.69 pp`，B/C/D 新规则平均只有 `+1.47 pp`。正式决策为 `HistoryDominatedGroupedEffect`。

M2C 固定预算结构重分配分支到此关闭：不设计在线成组重连，也不继续只为 A 回归优化组合信用。

### M1-R：新规则表征容量因果诊断

> 状态：已完成。正式记录见 [experiments/representation-capacity-v0.7.md](experiments/representation-capacity-v0.7.md)。

M1-R 保持 M1 任务、参数点、种子、训练预算和冻结动作读出，配对训练前/后隐藏线性探针、随机标签、原始输入与目标定向信用克隆。B/C/D 固定读出行为为 `55.3%`，训练前隐藏探针已经达到 `100.0%`，奖励训练的配对增益只有 `+0.04 pp`；目标定向信用行为为 `53.2%`。正式决策为 `LatentSymbolCodeWithoutRuleFormation`。

因此 M1 的“容量不足”不能继续解释为隐藏层缺少符号信息。外部探针为每条规则接受监督标签，只证明潜在信息可访问；系统本身没有形成足够的目标相关状态变化，也没有把它对齐到冻结读出。

### M1-F：目标相关状态变换形成

> 状态：已完成。协议通过，奖励调制节点扰动候选淘汰；正式记录见 [experiments/rule-formation-v0.8.md](experiments/rule-formation-v0.8.md)。

候选让每个隐藏单元在训练决策状态产生 `0.05` 零均值微扰，连接只用本地扰动、本地资格迹、资源和全局标量结果更新。开发阶段从 `[0.025, 0.05, 0.10, 0.20]` 选择 `0.20`；独立确认中 B/C/D 为 `48.3%`，相对现有基线 `-7.03 pp`，相对随机后果 `+0.98 pp [-0.33, 2.30]`，A 下降到 `62.2%`。全部数值和预算稳定，但没有后果特异性，正式决策为 `ConsequenceIndependent`。

不继续串行追加第二个启发式信用机制。M1-X 已在隔离克隆中完成目标可见 oracle 搜索，正式记录见 [experiments/reachability-v0.9.md](experiments/reachability-v0.9.md)。动作读出、节点、拓扑和 48 个槽位全部冻结：绝对边界 oracle 的 B/C/D 为 `97.3%`，相对冻结 `+54.43 pp`，相对乱序目标 `+41.98 pp`；固定子空间具有绝对表达能力。原参考范数包络 oracle 平均虽为 `73.2%`，最低新规则只有 `55.8%`，不能一致通过。正式决策为 `ReachableOnlyAtAbsoluteBounds`。

M1-XE 已用相同 oracle 完成统一参考范数扫描。开发与独立确认均在 `1.5×` 首次通过，确认 B/C/D 行为 `85.4%`、目标概率 `85.9%`、最低新规则 `70.8%`；`1.5×–4×` 是稳定通过窗口，而 `6×/8×` 再次失败。正式决策为 `UniformEnvelopeBoundaryConfirmed`。这表明原 `1.0×` 包络过紧，但不等于在线局部规则已经获得方向信用。

下一步 M1-NE 冻结 `1.5×`，只测试现有奖励局部调整能否利用新增自由度，并设置 `1.0×` 基线、`1.5×` 冻结、随机后果及 oracle 上界。若仍失败，则“幅度空间必要但不足”，瓶颈继续定位在信用方向或规则形成；只有产生后果特异收益并保持 A，才设计局部形成和维持目标范数的动态机制。在此之前不进入 M3、Scale 0，不增加节点、总连接数或可训练读出。正式记录见 [experiments/reachability-envelope-v1.0.md](experiments/reachability-envelope-v1.0.md)。

### 分支纪律

如果 M1 没有把失败定位到相应类别，就不进入该分支。一个分支失败时先记录机制淘汰，不同时加入下一个机制补救。

## 阶段 M3：调整机制地图

当某个 M2 分支通过后，冻结载体、机制和任务预算，重新测绘：

```text
P(能力向量达标 | 调整机制、机制参数、环境分布)
```

开发扫描寻找的不是单点最高分，而是适应、保留、迁移和稳定同时成立的连续区域。确认阶段使用未参与选择的系统种子、规则集合和输入编码，并加入机制关闭与信息量匹配对照。

只有跨相邻参数点形成可重复区域，机制才进入下一阶段；狭窄单点成功仍归为任务特化。

## 阶段 M4：跨任务迁移

冻结通过 M3 的调整机制，不重新调机制参数，依次改变：

- 输入编码；
- 规则数量和切换顺序；
- 输入—后果延迟；
- 动作接口；
- 受控导航或其他具身任务。

区分零样本迁移、少样本加速、重新学习和完全失败。只有主体行为使用了内部变化，才记为迁移；外部分类器能解码但主体没有使用，不算能力形成。

## 阶段 Scale 0：规模扫描

Scale 0 后移到 M3 形成连续稳定区域、且 M4 至少确认一种未见条件迁移之后。届时冻结调整机制、参数生成规则、任务和种子，扫描 `12、24、48、96、192` 单元，禁止按规模重新调参。

规模扫描回答的是通过验证的调整机制怎样随容量变化，而不是用更多节点补救尚未解决的遗忘或信用问题。结果仍区分容量改善、平台、方差增加和失稳。

## 暂缓项

- 不以补充树突、离子通道或分子细节作为默认进展；
- 不同时加入元可塑性、局部调制和结构生长；
- 不把复杂动画、混沌猜测或高维可解码性当作能力；
- 不在当前二元适应尚未扩展到保留与迁移前追求大规模网络；
- 不宣称调整机制“自然产生智能”，它仍是受约束、可证伪的算法设计。
