[package]
name = "neuro-engine"
version = "0.1.0"
edition = "2024"
description = "Deterministic embodied learning laboratory with retained neural simulation primitives"
license = "MIT"

[lib]
name = "neuro_engine"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
