# 可视化神经网络生命模拟器

> 文档定位：长期愿景与研究方向。本文件描述项目最终可能探索的范围，
> 不等同于当前开发清单。当前阶段的边界和验收要求以
> [MVP.md](MVP.md) 为准。

相关文档：

- [README.md](README.md)：文档入口与项目当前状态；
- [MVP.md](MVP.md)：第一阶段范围、关卡和停止条件；
- [MODEL_LIF.md](MODEL_LIF.md)：第一版神经元与突触模型；
- [ARCHITECTURE.md](ARCHITECTURE.md)：模拟、存储与界面边界；
- [EXPERIMENT_001.md](EXPERIMENT_001.md)：首个可重复实验；
- [decisions/0001-initial-technical-direction.md](decisions/0001-initial-technical-direction.md)：首批技术决策。

## 1. 项目愿景

构建一个可观察、可干预、可回放的生物启发式神经网络模拟器，用于探索：

- 神经元如何生长和建立连接；
- 信号如何产生、传播、衰减和汇合；
- 网络如何形成自维持活动；
- 外部刺激如何改变网络状态；
- 记忆是否可能存在于动态活动、传播路线和突触状态中；
- 修改单个神经元的理论模型后，整体网络行为如何变化。

项目不是普通的深度学习分类器，也不以“输入图片，直接输出标签”为首要目标。更合适的定位是：

> 神经系统时间数据库 + 模拟引擎 + 可视化调试器 + 实验平台。

“完整模拟真实神经元”不是第一阶段目标。系统应从可理解的简化模型开始，同时保持神经元、突触和学习机制可以替换及版本化。

## 2. 第一阶段目标

第一阶段优先证明网络级动力学成立：

> 构造一个复杂、可自维持、可复现、可观察并且可干预的脉冲神经网络。

这里追求的不是无约束随机混沌，而是接近临界状态的复杂动态或亚稳态。网络应满足：

1. 没有外部图片输入时，仍能维持一定活动；
2. 不会长期完全沉默；
3. 不会长期接近全网同步爆发；
4. 活动不会迅速退化成肉眼可见的简单固定循环；
5. 一次局部刺激能让后续网络轨迹产生可测量变化；
6. 刺激结束后能回到原动态范围，或进入另一个可解释的亚稳态；
7. 相同模型版本、参数、随机种子和输入事件产生相同结果。

第一阶段暂不要求：

- 图片识别；
- 语义理解；
- 完整神经元形态；
- 完整离子通道和细胞化学；
- 高精度树突计算；
- 大规模脑区模拟。

## 3. 最小神经元模型

第一版采用 LIF（Leaky Integrate-and-Fire，漏积分放电）模型。每个神经元至少包含：

- 唯一 ID；
- 空间位置；
- 神经元类型（兴奋性或抑制性）；
- 当前膜电位；
- 静息电位；
- 放电阈值；
- 膜电位衰减参数；
- 放电后的复位电位；
- 不应期；
- 可选的自发放电或起搏周期；
- 缓慢变化的稳态调节状态。

一个简化的离散更新形式为：

```text
V(t + dt) = V(t) + synapticInput(t) - leak(V(t))

if V >= threshold:
    emit spike
    V = resetPotential
    enter refractory period
```

每个模拟步的逻辑为：

```text
接收当前时刻抵达的突触事件
→ 计算突触电流
→ 更新并衰减膜电位
→ 检查放电阈值
→ 产生动作电位
→ 复位并进入不应期
→ 为所有输出突触创建在途事件
```

膜电位属于当前神经元，不会被原样传给其他神经元。神经元放电后发送的是动作电位事件，下游突触再根据自身权重和状态将其转换为突触电流。

## 4. 神经元与突触分离

神经元和突触必须是不同实体。一个神经元可以拥有大量输入和输出突触，同一对神经元之间也可以存在多个独立突触。

建议的数据结构：

```text
Neuron
├── id
├── position
├── type
├── membraneState
├── refractoryState
├── homeostaticState
└── modelSpecificState

Synapse
├── id
├── sourceNeuronId
├── targetNeuronId
├── targetCompartment
├── weight
├── excitatoryOrInhibitory
├── pathLength
├── conductionVelocity
├── synapticDelay
├── shortTermState
└── plasticityState
```

第一阶段建议规模：

- 100～300 个神经元；
- 约 80% 兴奋性神经元、20% 抑制性神经元；
- 每个神经元接收约 5～30 个突触；
- 使用稀疏循环连接；
- 使用少量起搏神经元启动并维持活动；
- 神经元阈值、衰减、延迟等参数带有少量个体差异；
- 所有随机过程由可保存的随机种子控制。

## 5. 信号传播与时间模型

神经动作电位不是电子以光速通过导线。它是由离子通道引起的局部膜电位变化沿轴突逐段再生，因此连接距离和传播速度会产生真实的时间延迟。

连接总延迟可以简化为：

```text
transmissionDelay = pathLength / conductionVelocity + synapticDelay
```

每条轴突或突触拥有在途事件队列：

```text
SpikeEvent
├── sourceNeuronId
├── synapseId
├── sendTime
├── arrivalTime
├── signalType
└── optionalDebugProvenance
```

较长轴突可以同时存在多个尚未抵达的脉冲，因而表现为一种短暂的延迟线或在途缓冲区。

系统需要区分三个时间概念：

- `simulationTick`：数值计算的离散步长；
- `eventTime`：脉冲发出、传播和抵达的模拟时间；
- `displayFrame`：可视化界面的刷新频率。

大脑不是全局同步的逐帧系统。模拟 tick 只是数值计算近似，显示帧也不应反向影响模拟结果。

## 6. 自维持与稳态机制

起搏神经元提供初始和持续驱动，循环连接使活动传播，抑制性神经元避免全网同步爆发。

如果仅靠固定网络难以维持稳定活动，可加入缓慢的稳态调节。例如：

```text
实际放电率高于目标范围 → 缓慢提高阈值
实际放电率低于目标范围 → 缓慢降低阈值
```

不同机制应在不同时间尺度上更新：

```text
膜电位和突触电流：每个 tick
阈值稳态：每 100 个 tick 左右
突触稳态：每 1,000 个 tick 左右
结构生长：更慢的时间尺度
```

稳态调节不能成为掩盖错误参数的万能补丁。系统必须允许关闭稳态机制，并对比固定参数、起搏驱动、噪声驱动和真正循环自维持之间的差异。

## 7. 动态记忆假设

项目将探索以下假设：

> 记忆可能部分存在于正在传播的信号、循环网络活动，以及暂时或长期改变的突触状态中。

潜在记忆载体包括：

1. **在途脉冲**：保存毫秒到几十毫秒的近期历史；
2. **循环网络活动**：脉冲模式在环路中持续传播；
3. **持续或间歇性群体活动**：可能构成工作记忆；
4. **短期突触状态**：即使停止明显放电，突触仍可能保留暂时痕迹；
5. **长期突触和结构变化**：保存长期的连接倾向和可再激活模式。

更准确的工作定义是：

> 记忆不是网络中保存的一份静态文件，而是网络被过去改变后，重新生成某种活动轨迹的能力。

可设计的实验包括：

- 关闭所有循环连接，测量在途信息多久完全消失；
- 添加不同长度的环路，测量活动模式可维持多久；
- 改变轴突速度和连接长度，观察网络节律变化；
- 检查不同延迟是否使多个脉冲在特定位置同步汇合；
- 让网络进入沉默状态，再用统一刺激测试旧模式能否重新出现；
- 删除特定神经元或连接，判断某种活动模式是否消失。

## 8. 外部视觉输入

第二阶段可以使用 `5×7`、`8×8` 或最大 `10×10` 的极简黑白图像。每个像素对应一个或多个视觉感受器。

输入流程为：

```text
图片像素
→ 感受器电流
→ 感受器膜电位变化
→ 感受器产生脉冲
→ 网络传播
```

网络不应直接接收“这是数字 6”这样的语义。未来可逐步加入：

- ON 感受器：亮度上升时活跃；
- OFF 感受器：亮度下降时活跃；
- 局部对比度感受器；
- 边缘和方向感受器；
- 事件相机式变化编码；
- 亮度到脉冲频率的编码。

同一输入可以使用不同读出器解释：

- 分类；
- 输入重建；
- 下一帧预测；
- 行为控制；
- 联想激活；
- 不进行语义解释，只观察内部动态。

主网络、感觉编码器和读出器应保持分离：

```text
Sensor:  外部输入 → 感觉事件
Brain:   感觉事件 + 当前状态 → 新状态 + 脉冲
Readout: 网络状态 → 分类、重建、动作或统计结果
```

## 9. 标签、信息与因果追踪

像素标签不会真实地沿神经网络传播。诸如 `P03`、`P17` 的标签只能存在于实验观察层，不能影响神经计算。

必须区分：

```text
真实模拟状态：膜电位、突触电流、脉冲、权重和延迟
调试归因状态：哪些输入可能对当前状态产生了多少影响
```

### 9.1 快速近似归因

每个神经元可以额外维护一个仅用于观察的来源贡献向量：

```text
Neuron N7 attribution
├── P02: +0.31
├── P13: +0.27
├── P14: +0.22
└── P05: -0.07
```

贡献可随膜电位衰减并沿突触传播，用于实时混色显示。它不参与真实动力学，并且在阈值、复位、抑制和多输入协同作用下只是近似解释。

### 9.2 反事实归因

由于输入图像很小，可以重复运行实验：

```text
完整输入运行一次
删除像素 P1 后重新运行
删除像素 P2 后重新运行
……
```

随后比较：

- 哪些神经元不再放电；
- 哪些放电延迟发生变化；
- 网络轨迹变化多大；
- 最终读出或行为是否改变。

这种方式测量的是删除某个因素后系统发生的真实变化，比假设标签随信号移动更可靠。

## 10. 可视化与实验工具

系统不能只有脉冲移动动画，而应成为一套“神经显微镜”。至少需要以下观察尺度：

1. 原始输入与感受器活动；
2. 单神经元膜电位、阈值、输入来源和不应期；
3. 局部网络的兴奋、抑制和信号传播；
4. 全网放电栅格图、活动热力图和活跃度曲线；
5. 群体状态随时间形成的低维轨迹；
6. 突触或轴突中的在途脉冲；
7. 输入来源的近似归因着色；
8. 刺激前后网络轨迹对比；
9. 神经元或突触消融后的反事实结果。

一个活动模式是否“有意义”，应使用可验证的操作性标准：

- 能否区分不同输入；
- 能否预测之后的输入或网络状态；
- 能否影响某种行为或读出；
- 能否重建部分输入属性；
- 是否在位置、亮度或噪声变化下保持稳定；
- 删除相应神经元或连接后，结果是否发生可重复改变。

可视化负责提出猜想，统计探针负责判断信息是否存在，干预实验负责判断其是否具有因果作用。

## 11. 计算、存储与回放

模拟计算与画面渲染必须完全分离。系统支持两种运行模式。

### 11.1 离线演化

```text
模型 + 初始状态 + 输入事件
→ 批量计算大量 tick
→ 保存检查点和事件流
→ 暂停、单步、倍速和拖动时间轴
```

### 11.2 交互实验

```text
读取目标时间前最近的检查点
→ 恢复网络状态
→ 在指定时间注入新刺激
→ 从该时间重新计算
→ 创建新的实验时间线分支
```

不要保存每个 tick 的完整网络副本。建议保存：

- 周期性完整检查点；
- 输入事件；
- 放电和脉冲抵达事件；
- 突触权重与连接变化事件；
- 生长和死亡事件；
- 必要的采样数据和统计指标；
- 模型版本、参数版本和随机种子。

目标时刻状态通过以下方式恢复：

```text
最近检查点 + 后续事件重放 = 目标时刻状态
```

确定性要求至少包括：

- 固定随机种子；
- 固定模型和参数版本；
- 固定输入事件；
- 固定同一时间事件的处理顺序；
- 固定数值精度和求解策略。

## 12. 可替换和版本化的理论模型

神经元模型应通过统一接口实现：

```text
NeuronModel
├── LIF
├── ContinuousDecayOrRateModel
├── AdaptiveSpikingModel
├── HodgkinHuxleyModel
└── CustomExperimentalModel
```

相同网络结构、随机种子和输入下，可以替换神经元模型并比较整体行为。

每次理论修改应创建新的模型版本或实验分支：

```text
Experiment
├── Model v1：LIF
├── Model v2：适应性阈值
├── Model v3：短期突触状态
├── Model v4：树突区室
└── Model v5：生长与代谢
```

不同模型版本产生的结果不能直接拼接为同一条时间线。若模型发生不兼容变化，应从兼容检查点或初始状态重新计算。

## 13. 推荐迭代阶段

### 阶段一：自维持网络

- LIF 神经元；
- 兴奋性与抑制性突触；
- 距离、传播速度和延迟；
- 在途脉冲队列；
- 起搏神经元；
- 可开关的简单稳态调节；
- 单步、暂停、倍速和固定种子重放；
- 网络活动基础图表；
- 局部刺激和消融实验。

### 阶段二：极简视觉输入

- `5×7` 或最大 `10×10` 黑白图片；
- 每像素感受器；
- 输入事件时间线；
- 输入来源近似归因；
- 逐像素反事实删除实验。

### 阶段三：动态记忆

- 短期突触可塑性；
- 循环活动检测；
- 沉默后重新唤起实验；
- 时间线分支和模型对比；
- 工作记忆与传播记忆指标。

### 阶段四：生长和复杂神经元

- 轴突和树突生长；
- 多区室神经元；
- 结构可塑性；
- 更真实的离子通道模型；
- 多尺度精度和细节层次（LOD）。

## 14. 第一阶段验收指标

第一阶段至少记录并评估：

- 全网平均放电率；
- 活跃神经元比例；
- 兴奋与抑制输入的平衡；
- 同步爆发比例和持续时间；
- 活动静默区间；
- 活动模式的周期性和重复程度；
- 一次局部刺激前后的状态轨迹距离；
- 刺激结束后的恢复时间；
- 删除某个神经元或突触后的变化；
- 相同种子重复运行的一致性。

验收不应仅依赖“动画看起来很复杂”。所有视觉现象都应能对应到数值指标和可重复实验。

## 15. 开放问题

后续设计需要继续回答：

1. 如何精确定义“自维持且不过度爆发”的验收区间；
2. 如何区分起搏驱动、随机噪声和真正的循环自维持；
3. 模拟使用固定 tick、连续时间事件队列，还是二者混合；
4. 每条轴突的在途脉冲需要建模到什么精度；
5. 稳态调节是否会掩盖模型本身的不稳定性；
6. 如何量化一次刺激引起的网络轨迹差异；
7. 来源归因如何处理阈值、复位和抑制造成的非线性；
8. 第一版采用二维网络图，还是空间化轴突动画；
9. 检查点、事件日志和时间线分支采用什么数据格式；
10. 如何设计神经元模型接口，使后续理论可以无痛替换。

## 16. 下一步任务

下一轮迭代应聚焦第一阶段，不扩展到图片识别或完整生物神经元：

> 将“可自维持的脉冲神经网络模拟器”细化为可实施的技术方案，明确模型公式、数据结构、tick/event 调度、自稳态机制、可视化布局、实验指标和阶段验收标准，同时保证模拟内核可替换、确定性和可回放。
