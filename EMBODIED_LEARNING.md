# 具身学习方向：最小可验证生命闭环

> 状态：当前主方向。旧 Phase 2 已证明固定随机点神经网络只能充当随机投影器，不能稳定产生可解释功能群。项目不再以“观察复杂放电”作为进展标准。

## 目标

从同一个未经训练的控制器出发，让一个二维主体通过环境后果改变连接，并在未见地图中比关闭学习的对照获得更多能量、食物和存活时间。

```text
环境 → 感觉 → 有状态神经控制器 → 动作
 ↑                              ↓
 └──────── 能量与行为后果 ────────┘
                    ↓
             奖励调制的突触可塑性
```

输出是主体对环境造成的动作，不由外部分类器解释。意义由行为后果定义。

## v1 系统边界

- 离散二维场地，包含食物、危险和边界；
- 主体具有位置、方向、能量、最近奖励和有限感受野；
- 动作为前进、左转、右转和进食；
- 控制器包含感觉输入、带泄漏状态和适应项的内部单元、动作单元；
- 动作突触使用带资格迹的三因素可塑性：突触前活动、动作选择误差、后果信号；
- 训练地图与测试地图使用不重叠种子；
- 每个机制都可以关闭，随机数和输出完全确定；
- Web 只展示世界轨迹、能量、动作概率、内部活动和学习曲线。

v1 不宣称模拟真实生命、意识或完整生物神经元。它只建立第一个可以从外部行为直接验证的感知—学习—动作闭环。

当前学习算法的公式、与 Reservoir Computing/策略梯度/三因素规则的关系，以及尚未证明的能力，集中记录在 [LEARNING.md](LEARNING.md)。下一阶段不会默认增加生物复杂度，而会依照 [ROADMAP.md](ROADMAP.md) 验证跨单元循环动力学。

Gate A 已完成奖励与感觉辅助审计，结果见 [experiments/gate-a-v1.1.md](experiments/gate-a-v1.1.md)。Gate B 已完成状态必要性与鲁棒性边界，结果见 [experiments/gate-b-v1.2.md](experiments/gate-b-v1.2.md) 和 [experiments/gate-b-robustness-v1.2b.md](experiments/gate-b-robustness-v1.2b.md)。Gate C 也已证明资格迹在受控延迟能量任务中的功能，见 [experiments/gate-c-v1.3.md](experiments/gate-c-v1.3.md)。下一阶段只进入 Gate D 的内部循环连接实验。

## 验收标准

正式报告必须同时给出：

1. 未训练控制器；
2. 经历同样训练但关闭可塑性的控制器；
3. 学习后的控制器；
4. 打乱学习后动作突触的控制器；
5. 移除最重要内部特征的控制器。

学习成立至少需要：

- 学习后权重确实发生持久变化；
- 未见地图平均获取食物数超过关闭学习对照；
- 提升不是仅来自训练地图记忆；
- 打乱已学习连接会削弱表现；
- 相同配置和种子生成完全相同报告与轨迹。

如果这些条件不成立，保存负结果并调整单一机制；不得用更漂亮的动画代替证据。

## 权威输出

- `cargo run --release --example embodied_lab` 生成的版本化 JSON；
- Rust 测试中的确定性、能量守恒边界和学习对照；
- Web 行为仪表盘；
- 实验报告中的未见地图指标。

旧随机网络和 3D 回放代码不再属于产品主路径。Git 历史保留其实现，文字实验报告保留其失败结论。
