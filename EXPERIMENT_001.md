# Experiment 001：延迟兴奋/抑制网络的活动

## 1. 目的

建立第一套可重复的基线实验，回答：

1. 网络在无驱动时是否自然沉默；
2. 起搏驱动能否维持活动而不导致长期全网同步；
3. 关闭起搏后，循环活动还能持续多久；
4. 局部刺激是否会造成可测量、可重复的轨迹差异；
5. 同一配置是否能够确定性重放。

本实验不证明记忆、智能或生物真实性。

## 2. 固定条件

首轮建议从以下规模开始，具体数值仍属暂定：

```text
神经元数量        100
兴奋/抑制比例     80/20
每个神经元入度    5～20
网络              稀疏、有向、循环
神经元模型        MODEL_LIF.md 定义的版本
结构随机种子      固定并记录
运行随机种子      固定并记录
```

所有电位、时间、速度、距离和权重参数必须由配置文件提供单位。试运行期间可以调参，但每次调整都产生新的实验配置摘要。

## 3. 实验分组

### A：无驱动基线

- 无起搏；
- 无随机噪声；
- 只从一个明确的初始化刺激开始；
- 测量活动衰减和最终沉默时间。

### B：起搏驱动

- 使用固定、可记录的起搏事件；
- 测量放电率、活跃比例、同步爆发和静默区间；
- 判断活动是否完全由起搏节律锁定。

### C：撤除驱动

- 先按 B 运行到预定时刻；
- 关闭起搏；
- 测量最后一次起搏后活动持续时间、周期性和衰减方式。

### D：局部刺激

- 从 B 的同一检查点建立控制分支和刺激分支；
- 只在刺激分支对固定空间区域注入相同事件；
- 比较后续状态轨迹和恢复时间。

### E：消融

- 从相同检查点建立分支；
- 删除一个选定神经元或一组突触；
- 使用与控制分支完全相同的后续输入；
- 比较活动和轨迹变化。

## 4. 必须记录的指标

- 全网及分类型平均放电率；
- 固定窗口内活跃神经元比例；
- 兴奋与抑制到达事件的数量和加权总量；
- 群体同步爆发的次数、规模和持续时间；
- 静默区间分布；
- 放电序列的自相关和候选周期；
- 最后一次驱动后活动持续时间；
- 控制与干预分支的状态距离；
- 干预后回到基线范围所需时间；
- 完整事件日志的确定性摘要。

轨迹距离的具体定义尚未决定。第一版可同时保留分箱放电向量距离和低维投影距离，在试运行后选择一个主指标。

## 5. 验收区间的制定

不在实现前伪造“生物正确”的固定数值。采用两步法：

1. 运行一组公开、版本化的参数扫描，保留所有结果；
2. 根据沉默、稳定活动和持续爆发三类明显状态，冻结一套工程验收区间。

冻结后的区间属于 `experiment-001/v1`，后续不能为使新代码通过而静默修改。需要调整时创建 `v2` 并说明原因。

无论阈值如何校准，以下条件直接判定失败：

- 结果依赖显示帧率；
- 相同运行的事件摘要不一致；
- 出现零时间无限事件循环；
- 日志无法定位一次放电的直接输入事件；
- 起搏、噪声和循环维持无法在实验记录中区分；
- 只根据 3D 动画宣布网络“有意义”。

## 6. 输出物

一次完整实验至少生成：

- 完整配置和单位；
- 模型、格式和程序版本；
- 网络结构摘要；
- 输入和干预事件；
- 分块事件日志；
- 必要检查点；
- 指标表；
- 栅格图与活动曲线；
- 结果摘要，包括失败结果；
- 可供 3D 查看器读取的回放数据。
