# 最小动态基座审计

## 定位

项目不以复刻真实神经元为可验证目标。当前“神经元”是一个操作性动态节点，只用于研究哪些最小条件足以形成稳定、可学习的宏观功能。

不能把生物细节数量当作真实性，也不能用扩大节点数掩盖小规模机制冲突。后续先审计基座，再单独研究规模。

## 最小基座契约

一个节点只承诺具备以下可观察能力：

- 保存有限的局部状态；
- 接收并传播有时序的信息；
- 以局部规则改变有限连接；
- 为活动和可塑性消耗局部资源；
- 从持续、无任务信息的供给中恢复资源；
- 在长期运行中暴露活动、资源、权重和输出状态。

这些是实验接口，不是对真实细胞结构的声明。树突形态、离子通道、胶质细胞和分子通路只有在现有指标定位到对应缺口后，才允许作为单独机制加入。

## 执行顺序

### Map 2A：内生有界可塑性

保持 24 节点、Map 1 的结构、种子、探针和阈值不变，只把加性循环权重更新替换为一种权重依赖、内生有界的局部规则。首个候选限定为软边界或 Oja 类规则，二者必须在开发扫描前择一冻结。

成功条件是权重漂移下降，同时重复切换或损伤恢复不退化。失败则淘汰该规则，不进入下一阶段。

### Map 2B：持续基础供能

在通过 Map 2A 的机制上只加入资源闭环。基础供能持续存在、不携带任务信息，也不直接注入膜电位或固定激活任何节点。活动、放电和权重改变消耗资源；资源不足只能通过统一的局部调制降低活动与可塑性。

不加入睡眠阶段、休息状态机、预设节律或外部起搏周期。静默、爆发、轮换和周期只能作为系统自然演变的观察结果。

### Map 2C：持续输入压力测试

冻结前两阶段机制，取消清晰的训练—休息分段，让任务输入和环境条件连续变化。验证系统能否在持续运行中保持资源、活动和权重有界，并继续适应，而不是依赖人工重置。

### Scale 0：节点数量扫描

只有 Map 2A～2C 形成连续稳定区域后才执行。冻结全部机制、参数范围、任务和种子生成规则，扫描 `12、24、48、96、192` 节点，禁止按规模重新调参。

Map 2A～2C 已于 `learnability-map/v0.5-continuous-stream` 完成并通过串行冻结条件：2A 的软边界降低漂移，2B 的持续供能形成可干预资源约束，2C 在无状态重置连续流中确认 6 点稳定区域。机制上的原 Scale 0 前置条件已经满足；但根据 [ADR 0006](decisions/0006-dynamic-adjustment-mechanisms.md)，Scale 0 的研究优先级后移，先验证当前调整机制能否保留多种规则并迁移到至少一种未见条件。

规模结果分为：

- 平滑改善：容量效应；
- 很快平台：当前机制的容量边界；
- 方差或失稳随规模增加：扩展失败；
- 功能形成概率与动力学指标在同一区间共同转变：规模效应候选，需要独立确认。

## 统一约束

每一阶段只能增加或替换一个机制。继续复用开发/确认种子隔离、同种子配对、因果关闭对照和正式数据导出。画面用于解释已经定义的指标，不能代替形成概率、跨种子复现和干预证据。

只要小规模系统仍没有稳定区域，就不进入大规模网络、完整脑结构或意识相关叙事。
