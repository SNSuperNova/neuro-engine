# Gate B 冻结规格：延迟线索岔路任务

## 状态

规格已冻结并完成实现。验收条件没有在实现或调参后修改；正式结果见 [experiments/gate-b-v1.2.md](experiments/gate-b-v1.2.md)。

## 为什么选择这个任务

Gate A 证明无距离塑形仍可学习，也证明随机遮挡下仍有行为，但随机遮挡不能说明主体是否真正使用历史：当前方向信号在多数时刻仍可重新出现。

Gate B 必须创造一个当前感觉完全相同、但正确动作取决于过去线索的决策点。这样无状态控制器原则上不能超过机会水平，泄漏状态若有功能则必须保留已经消失的信息。

## 环境

任务名：`delayed-cue-fork/v1`。

- 主体从直线走廊底部出发，朝向保持不变地接近 T 型岔路；
- 回合开始的前 2 步显示左或右二元线索；
- 之后线索永久关闭，至少经过 4 个无信息步骤才到达岔路；
- 左右分支外观、长度、危险和局部感觉完全对称；
- 只有线索指示的一侧包含高能量食物，另一侧是无奖励终点；
- 左右目标在训练和测试中独立、均衡、伪随机分配；
- 不提供最近食物方向，不提供距离塑形；
- 奖励只来自真实能量变化；
- 动作仍是前进、左转、右转和进食。

测试地图与训练序列使用不重叠种子。走廊几何可以做确定性镜像变化，但不得让墙纹理、步数奇偶或随机数顺序泄露正确分支。

## 三个匹配控制器

1. `stateless`：当前感觉经过固定随机特征后直接读出，不保留历史；
2. `state-reset`：与当前控制器参数完全相同，但每一步在读入感觉前清空隐藏和适应状态；
3. `leaky-state`：当前泄漏状态和适应控制器。

三者必须具有相同感觉接口、动作接口、可训练动作连接数、训练回合和随机种子。不得给 `leaky-state` 额外输入或更多可训练参数。

## 防泄漏对照

- `cue-visible-at-fork`：在岔路重新显示线索，证明三种控制器都有能力学习动作映射；
- `cue-randomized`：线索与食物位置独立，三者应接近 50% 机会水平；
- `history-shuffled`：评估时交换不同回合的早期线索和后半轨迹，若历史确实被使用，性能应下降；
- 左右镜像分别统计，排除固定转向偏好。

## 统计协议

- 12 个独立模型种子；
- 每个模型 1,200 个训练回合；
- 每个模型 200 个均衡测试回合；
- 推断单位是模型种子；
- 对同一种子做配对差，并报告 95% Student-t CI；
- 主指标：在岔路选择线索指示分支的比例；
- 次指标：获取食物、最终能量、达到 75% 正确率所需回合、左右偏置和运行时间。

## Gate B 验收

必须同时满足：

1. `cue-visible-at-fork` 中三种控制器都达到至少 80% 正确率；
2. `cue-randomized` 中三种控制器的跨种子区间包含 50%；
3. 延迟线索任务中 `leaky-state − state-reset` 的正确率配对增益均值至少为 15 个百分点，且 95% CI 下界大于 0；
4. `leaky-state` 正确率至少为 70%；
5. `history-shuffled` 显著削弱 `leaky-state`；
6. 结论在左右镜像和未见测试序列中成立；
7. 相同配置与种子生成完全相同报告。

如果 `leaky-state` 不能通过，不增加神经元规模。先报告负结果，再检查状态时间常数、动作反馈缺失或任务时间尺度是否匹配。

## 已实现的任务细节

- 走廊是 6 步强制前行段。前 2 步有线索，之后 4 步无信息，第 7 次决策才可选择分支；这移除了与状态假设无关的稀疏奖励导航混杂；
- 四个动作始终由控制器输出，但强制走廊中动作不改变轨迹；到达岔路后只有左转或右转提交分支，正确终点仍需执行进食；
- 观察者画面会标出食物在哪一侧，但控制器 12 个输入中没有目标位置、最近食物方向或距离；
- 24 个状态单元的固定输入投影与 96 条可训练动作连接在三种控制器间完全相同；只有状态保留规则不同；
- Gate B 使用 `hidden_leak = 0.80`，使状态时间尺度覆盖 4 步无信息间隔。该值先在独立开发种子上冻结，最终 12 个模型种子使用新的基础种子；
- 正式 JSON 不包含墙钟耗时，确保逐字节可重复。命令行单独输出非权威运行基准。

## 正式结论

在未参与机制选择的 12 个模型种子上，`leaky-state` 延迟线索正确率为 `81.8%`，95% CI `[73.6%, 90.0%]`；`state-reset` 为 `51.2%`。同种子配对增益为 `30.6` 个百分点，95% CI `[22.6, 38.6]`。历史置乱将正确率降至 `48.8%`，配对下降为 `33.0 [25.4, 40.6]` 个百分点。全部七项冻结验收通过。
