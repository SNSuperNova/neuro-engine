# Gate B 鲁棒性边界冻结规格

> 状态：已完成。可靠记忆边界为 4 步；正式结果见 [experiments/gate-b-robustness-v1.2b.md](experiments/gate-b-robustness-v1.2b.md)，权威数据为 `app/public/gate-b-robustness-v1.2b.json`。

## 目的

Gate B 已证明当前连续状态在 4 步无信息延迟下有用。本审计不重新决定 Gate B 是否通过，也不修改其验收阈值；它只回答成功依赖多长的时间尺度和多强的人工线索投影。

## 固定项

- 仍使用 `delayed-cue-fork/v1`、`leaky-state`、24 个状态单元和 96 条可训练动作连接；
- 保持学习率、资格迹、适应、训练回合、动作、奖励和强制走廊规则不变；
- 奖励仍只是真实能量变化；
- 每个条件使用 12 个独立模型种子、1,200 个训练回合和 200 个均衡测试回合；
- 正式边界种子不参与 Gate B 机制选择。

## 单因素扫描

基线是：无信息延迟 `4` 步、线索投影尺度 `1.20`、持续感觉投影尺度 `0.20`。

1. 延迟：`2 / 4 / 6 / 8 / 12` 步；
2. 线索投影：`0.60 / 0.90 / 1.20 / 1.50`；
3. 持续感觉投影：`0.10 / 0.20 / 0.40 / 0.65`。

每次只改变一个因素。重复的基线只计算一次，共 11 个唯一条件。不得根据结果追加更有利的点后仍把它们称为冻结扫描。

## 报告

- 正确分支比例、食物比例、最终能量、左右正确率和达到 75% 所需回合；
- 每项报告跨模型种子的 95% Student-t CI；
- “可靠记忆边界”定义为正确率均值至少 70%，且 95% CI 下界大于 50% 的最大延迟；
- 分别报告线索强度与持续干扰的性能曲线，不用单个最好配置替代完整扫描；
- 相同配置和种子必须生成完全相同的报告。

该审计允许得到负边界；任何失败都不会追溯性否定或放宽 Gate B。

## 冻结结果

- 4 步：`86.2% [83.3%, 89.2%]`，满足可靠边界；
- 6 步：`60.0% [54.1%, 65.9%]`，未达到 70% 阈值；
- 8 与 12 步接近机会水平；
- 线索尺度降低与持续输入增强均削弱表现，持续尺度 0.40 还出现明显左右偏置。
