# Gate C 冻结规格：延迟能量与资格迹

> 状态：已完成，全部冻结验收通过。正式结果见 [experiments/gate-c-v1.3.md](experiments/gate-c-v1.3.md)，权威数据为 `app/public/gate-c-v1.3.json`。

## 研究问题

Gate B 证明了状态能保存早期信息，但食物能量只比岔路动作晚约一步，尚未证明动作资格迹承担了跨时间责任分配。Gate C 必须让正确动作由当前感觉直接确定，同时把真实能量后果延迟到账，从而隔离资格迹。

## 任务

任务名：`delayed-energy-fork/v1`。

- 每回合从对称岔路开始，左或右线索在岔路当前可见；
- `turn-left / turn-right` 提交分支，`forward / eat` 不提交并产生真实碰撞消耗；
- 提交后进入观察完全相同的强制等待期，四个动作仍输出但不改变等待进程；
- 等待结束时，正确分支的食物能量一次性进入主体，错误分支不获得能量，随后两者同时终止；
- 延迟为 0 时，正确分支能量在提交动作的同一步到账；
- 等待期间线索关闭，不提供选择结果、目标位置、距离、食物方向或正确性；
- 奖励严格等于真实能量变化除以食物能量，不使用塑形或额外标签；
- 左右目标在训练和测试中独立、均衡、伪随机分配。

## 控制器与比较

统一使用无状态控制器，避免短期状态替资格迹保存目标。所有条件共享：

- 12 个当前感觉、24 个固定随机特征单元、4 个动作；
- 相同初始固定投影和 96 条可训练动作连接；
- 相同学习率、温度、奖励基线、训练回合、目标序列和动作随机种子。

只改变 `eligibility_decay`：`0.00 / 0.50 / 0.88 / 0.97`。奖励延迟扫描为 `0 / 2 / 4 / 8` 步，形成冻结的 4×4 因子矩阵。

另设 `cue-randomized` 防泄漏对照：在延迟 4、资格迹 0.88 下使线索与目标独立。

## 统计协议

- 12 个独立、未参与机制选择的正式模型种子；
- 每个模型和矩阵单元训练 1,200 回合，使用 200 个均衡测试回合；
- 推断单位是模型种子，同种子比较使用配对 95% Student-t CI；
- 主指标是在岔路选择线索指示分支的比例；
- 次指标是能量到账比例、最终能量、达到 75% 正确率所需回合和左右偏置；
- 墙钟基准只由命令行单独输出，不写入确定性 JSON。

## Gate C 验收

必须同时满足：

1. 即时能量条件中四种资格迹都达到至少 80%，证明它们都有学习当前线索映射的能力；
2. 延迟 8 步时，当前 `0.88` 相对 `0.00` 的正确率配对增益均值至少 15 个百分点，且 95% CI 下界大于 0；
3. 延迟 8 步、资格迹 0.88 的正确率至少 70%；
4. `eligibility_decay = 0` 从即时到延迟 8 步显著退化；
5. 延迟 4 的随机线索对照 95% CI 包含 50%；
6. 结论在左右目标和未见测试序列中一致，分支提交率至少 95%；
7. 相同配置和种子生成完全相同报告。

若当前 0.88 不能通过，不更改上述阈值。先报告负结果，再判断有效资格迹时间尺度是否更接近 0.97；这可以形成能力边界，但不能把 0.97 的结果冒充预注册主比较。

## 冻结结果

- 延迟 8、trace 0.88：`99.3% [98.7%, 99.9%]`；
- 延迟 8、trace 0：`47.0% [41.3%, 52.6%]`；
- 主配对增益：`52.3 [46.5, 58.1]` 个百分点；
- 随机线索对照：`49.8% [48.5%, 51.0%]`；
- 左右目标一致、分支提交率 100%、结果确定性通过。
