# Gate D 冻结规格：稀疏内部循环动力学

> 状态：已完成，全部冻结验收通过。正式结果见 [experiments/gate-d-v1.4.md](experiments/gate-d-v1.4.md)，权威数据为 `app/public/gate-d-v1.4.json`。

## 研究问题

Gate B 证明固定自递归状态在 4 步无信息延迟中有用，同时把当前可靠边界定在 4 步；6 步降到 60%，8 步接近机会水平。Gate D 只增加隐藏单元之间的固定稀疏循环连接，检验跨单元动力学能否延长记忆，而不改变感觉、动作、学习规则或可训练参数预算。

## 任务与扫描

- 继续使用 `delayed-cue-fork/v1` 的强制走廊、二元线索、对称岔路与真实能量奖励；
- 线索固定显示 2 步，扫描无信息延迟 `4 / 6 / 8 / 12`；
- 每个条件训练 1,200 回合，使用 200 个左右均衡的未见测试回合；
- 12 个独立正式模型种子；
- 推断单位是模型种子，同种子比较报告配对 95% Student-t CI；
- Gate C 必须继续作为全量回归测试，不在 Gate D 中改变资格迹。

## 三个等接口控制器

三者共享 12 个感觉、24 个状态单元、4 个动作、288 个固定感觉投影、96 个可训练动作权重、初始化、训练序列和动作随机数。

1. `no-recurrence`：保留 Gate B 的单元自身泄漏与适应，跨单元循环矩阵为零；
2. `shuffled-recurrence`：拥有与结构化网络完全相同的非零权重集合、稀疏度、兴奋/抑制边数与谱半径，但把隐藏单元身份做固定置乱，破坏循环结构与线索投影的对应；
3. `structured-recurrence`：按单元对左右线索的固定投影偏好形成两个程序集群，同偏好连接为正、异偏好连接为负。

循环权重固定，不参与学习。因而三者可训练参数数都为 96；`structured` 与 `shuffled` 还具有完全相同的 144 条非零循环连接和相同权重多重集合。主因果比较是 `structured − shuffled`，不是只比较有连接与无连接。

## 冻结动力学

- 每个状态单元接收 6 条非自身循环连接，矩阵稀疏度为 `144 / (24 × 23) = 26.1%`；
- 正边为兴奋性边，负边为抑制性边；当前版本明确采用“边符号”，不宣称满足 Dale 单细胞符号定律；
- 结构化矩阵生成后缩放到谱半径 `0.15`；
- 自身泄漏仍为 Gate B 的 `0.80`，适应参数、感觉尺度与动作学习参数不变；
- 状态更新为：

```text
h_i(t) = tanh(input_i(t) + 0.80 h_i(t-1)
              + Σ_j recurrent_weight[i,j] h_j(t-1)
              - adaptation_strength × adaptation_i(t-1))
```

- 为避免更新顺序引入隐式异步差异，所有循环项只读取完整的上一时刻状态；
- 谱半径由确定性幂迭代估计并记录，允许误差 `±0.03`。

## 行为与稳定性指标

行为指标：正确分支比例、食物比例、最终能量、左右目标正确率、达到 75% 滑动窗口正确率所需回合，以及每种控制器的可靠记忆边界。

状态指标在评估轨迹的岔路决策前统计：

- `mean_absolute_activity`：平均 `|h|`；
- `saturated_unit_fraction`：`|h| ≥ 0.95` 的单元比例；
- `silent_unit_fraction`：整个无信息段平均 `|h| ≤ 0.05` 的单元比例；
- `population_synchrony`：每一步群体均值绝对值除以群体平均绝对活动，再跨步平均；
- `finite_state_fraction`：有限状态值比例。

冻结的稳定范围：有限比例必须为 100%，平均绝对活动在 `[0.05, 0.90]`，饱和比例不超过 25%，静默比例不超过 60%，群体同步不超过 90%。

## Gate D 验收

必须同时满足：

1. 延迟 8 时，`structured-recurrence − shuffled-recurrence` 的正确率配对增益均值至少 15 个百分点，且 95% CI 下界大于 0；
2. 延迟 8 时结构化循环正确率至少 70%，左右目标正确率都至少 70%，分支提交率至少 95%；
3. 结构化循环的可靠记忆边界至少为 8 步，并严格长于置乱循环；
4. 置乱循环相对无循环不得出现同等主收益：其延迟 8 配对增益的 95% CI 必须包含 0，或均值低于 10 个百分点；
5. 结构化循环在上述五项状态指标上全部处于冻结稳定范围；
6. 结构化与置乱控制器的循环权重多重集合、非零连接数、正负边数和谱半径匹配，可训练参数完全相同；
7. 相同配置与种子生成完全相同的报告和轨迹。

若开发结果未通过，不修改上述验收阈值。可以在正式种子运行前修正明显实现错误或在文档中登记机制参数变化；一旦正式基础种子 `0x474154455f440201` 开始运行，不再调节参数。

## 正式运行前的机制修订记录

最初候选谱半径为 0.55。独立开发种子显示，置乱循环在延迟 8 时也达到 87.7%，结构化优势只有 10.7 个百分点；这不能排除“任意循环只是增加有效时间常数”。冻结验收阈值保持不变，开发扫描只比较 `0.15 / 0.25 / 0.35 / 0.45`。选择最低且满足全部开发验收的 0.15；在随后固定为逐回合相同动作随机流的最终开发复核中，置乱循环延迟 8 为 56.0%，结构化循环为 84.5%，可靠边界分别为 6 和 8 步。完成此记录后才允许运行正式基础种子。

## 冻结结果

- 延迟 8：结构化循环 `80.6% [70.4%, 90.9%]`，置乱循环 `56.2% [49.7%, 62.6%]`；
- 主配对增益：`24.5 [14.3, 34.6]` 个百分点；
- 置乱循环相对无循环：`3.2 [-3.9, 10.3]` 个百分点；
- 可靠记忆边界：无循环 4 步、置乱循环 4 步、结构化循环 8 步；
- 有限状态 100%，平均活动、饱和、静默和同步均通过冻结范围。
