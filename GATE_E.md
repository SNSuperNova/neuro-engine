# Gate E 冻结规格：连续状态与 LIF 脉冲状态等预算比较

> 状态：已完成，全部冻结验收通过。正式结果见 [experiments/gate-e-v1.5.md](experiments/gate-e-v1.5.md)，权威数据为 `app/public/gate-e-v1.5.json`。

## 研究问题

Gate B～D 已分别证明连续状态、资格迹和固定循环结构能解决受控时间问题。Gate E 不增加可塑内部连接，而是把保留的事件式 LIF 神经元接入同一个行为闭环，回答一个更窄的问题：在感觉、动作、训练、可训练连接数和动态状态预算匹配时，脉冲表示是否改善记忆、事件稀疏性、运行成本或损伤鲁棒性中的至少一项。

画面更像神经元不属于验收证据。

## 冻结任务与统计单位

- 沿用 `delayed-cue-fork/v1` 的强制走廊、二元线索、对称岔路和真实食物能量；
- 线索显示 2 步，扫描无信息延迟 `4 / 8 / 12`；
- 每个条件训练 1,200 回合，评估 200 个左右均衡的未见序列；
- 12 个独立正式模型种子；
- 推断单位为模型种子，同种子差异使用配对 95% Student-t CI；
- 三个控制器逐回合共享目标序列和动作随机数流。

## 三个等预算控制器

三者共享 12 个感觉、24 个内部单元、4 个动作、288 个固定感觉投影和 96 个可训练动作读出权重。

1. `stateless`：每步只计算当前固定投影的 `tanh` 特征；
2. `continuous-state`：24 个连续状态及 24 个适应状态；
3. `lif-spiking`：24 个事件式 LIF 膜电位及 24 个低通脉冲迹。

连续与 LIF 都按 48 个任务信息动态标量、384 字节 `f64` 概念状态载荷核算。实现内存另行报告：当前 Rust 连续状态为 384 字节，LIF 的 `LifNeuron` 还保存参数、整数时间和不应期，连同脉冲迹为 2,496 字节。两者都不包含共享权重、容器头、分配器或进程 RSS。

## 冻结动力学与时间匹配

连续状态：

```text
h_i(t) = tanh(input_i(t) + 0.86 h_i(t-1) - adaptation_i(t-1))
```

LIF 状态：

- 环境一步对应 10 ms 模拟时间；
- 每步每单元接收一个聚合后的带符号电压事件，共 24 个外部事件/步；
- 静息与复位电位 0 mV，阈值 1 mV，膜时间常数 30 ms，绝对不应期 5 ms；
- 事件之间由 `LifNeuron` 解析衰减，不引入神经动力学固定 tick；
- 动作读出和资格迹读取低通脉冲迹：

```text
z_i(t) = 0.86 z_i(t-1) + spike_i(t)
```

连续泄漏与脉冲迹衰减都冻结为 0.86，避免把更长时间常数误当成脉冲格式收益。LIF 膜电位和脉冲迹对应连续控制器的状态和适应两个动态标量；只有相同的 96 个动作读出权重参与训练。

## 损伤、事件、时间与内存协议

- 训练结束后，用模型种子确定一个包含 6/24 单元的固定掩码；
- 三个控制器使用同一掩码，评估时才把对应读出特征置零，不重新训练；
- 延迟 8 同时报告未损伤正确率、损伤后正确率及同种子下降；
- 记录环境步、外部输入事件、发放脉冲、稠密输入乘加和稠密读出乘加；
- 平台时间单独写入 `experiments/gate-e-runtime-windows-x86_64.json`，不进入确定性科学 JSON 或验收；
- 当前实现仍对 288 个感觉投影和 96 个动作读出做稠密乘加。脉冲稀疏不等于当前 CPU 实现已经获得稀疏计算收益。

## 冻结验收

必须同时满足：

1. 三者感觉、状态单元、动作、固定输入权重和可训练动作权重预算相同；连续与 LIF 的动态标量预算相同；
2. 延迟 8 的 LIF 正确率至少 70%，左右目标都至少 65%，分支提交率至少 95%；
3. LIF 至少改善一个预定义轴：
   - 记忆：延迟 8 相对连续状态的配对增益至少 10 个百分点且 95% CI 下界大于 0；或
   - 损伤：25% 损伤后的正确率下降至少比连续状态少 5 个百分点；
4. LIF 状态全部有限，记忆段平均脉冲数大于 0 且不超过 12 脉冲/步，静默单元比例不超过 80%；
5. 三类控制器都完成相同的 25% 损伤协议；
6. 相同配置与种子生成完全相同的报告和轨迹。

事件稀疏、运行时间和内存必须报告，但不会在没有真实计算收益时被算作“改善轴”。

## 正式运行前修订记录

首个开发版本错误地保留连续泄漏 0.80，却让 LIF 脉冲迹使用 0.86。虽然当时 LIF 延迟 8 达到 82.3%，该比较混入了时间常数差异，因而被废止且没有作为正式证据。

随后将两者统一为 0.86。匹配后的独立复核中，延迟 8 连续状态为 67.3%，LIF 为 88.5%，配对差仍为 21.2 个百分点。完成该修订记录后，才使用正式基础种子 `0x474154455f450201` 生成权威快照；正式运行后未再调整参数或阈值。

## 冻结结果

- 延迟 8：LIF `88.5% [85.3%, 91.6%]`，连续状态 `67.3% [60.5%, 74.1%]`，无状态 `49.5% [48.2%, 50.9%]`；
- LIF 相对连续状态的配对增益 `21.2 [13.6, 28.7]` 个百分点；
- 可靠记忆边界：无状态无可靠点、连续状态 4 步、LIF 12 步；
- LIF 延迟 8 左右目标正确率分别为 87.3% 和 89.6%，所有试验都提交分支；
- 25% 损伤后 LIF 从 88.5% 降至 80.5%，下降 `8.0 [3.7, 12.2]` 个百分点；连续状态下降 4.0 个百分点。因此损伤鲁棒性没有改善；
- LIF 在记忆段平均 0.80 脉冲/步，约占 24 单元的 3.3%；但每环境步仍编码 24 个外部事件并执行相同的稠密输入和读出乘加；
- 连续与 LIF 的概念状态载荷同为 384 字节，但当前 Rust 实现动态状态分别为 384 和 2,496 字节；当前 Windows x86-64 release 基准中，LIF 约 2412 ns/环境步，连续状态约 385 ns/环境步，LIF 慢约 6.3 倍。

Gate E 因记忆轴通过，但当前 LIF 不升级为默认控制器：它是功能明确、代价也明确的实验分支，而不是免费获得的“类脑优势”。
