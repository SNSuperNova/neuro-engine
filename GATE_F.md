# Gate F 冻结规格：内部可塑性与规则反转

> 状态：已完成，全部冻结验收通过。正式结果见 [experiments/gate-f-v1.6.md](experiments/gate-f-v1.6.md)，权威数据为 `app/public/gate-f-v1.6.json`。

## 研究问题

Gate A～E 分别排除了任务捷径、证明了时间状态与资格迹的作用，并比较了固定循环和 LIF 表示。它们的感觉投影与内部连接在训练后都保持固定。Gate F 只回答一个新问题：当环境中“线索对应哪一侧食物”的规则发生反转，冻结动作读出的主体能否仅靠一种内部可塑性重新适应。

本 Gate 不增加神经元数量、不开放动作读出再训练、不引入 LIF，也不同时叠加多种生物机制。

## 冻结任务

任务为 `reversal-cue-fork/v1`：

- 12 个感觉通道、24 个连续内部单元、左右两个分支动作；
- 线索显示 2 步，随后经过 4 步无信息延迟，再选择分支；
- 原始规则为“左线索→左分支、右线索→右分支”；
- 完成原始规则训练后冻结 48 个动作读出权重；
- 环境随后切换为反转规则，最后再恢复原始规则；
- 每个适应阶段 800 回合，在 `0 / 100 / 200 / 400 / 800` 回合冻结副本并用 200 个左右均衡序列评估；
- 12 个独立模型种子；同一模型种子的所有控制共享初始化、训练序列、评估序列和动作随机数流。

奖励只有选择结果：正确 `+1`，错误 `-1`。不存在目标标签监督更新、距离塑形或动作读出再训练。

适应阶段使用冻结的 16% 均匀动作探索，四个控制共享相同随机数；评估阶段关闭探索。这个干预只保证反转后仍能观察另一个动作的后果，不携带正确分支信息。

## 四个控制器

四者从同一已训练模型克隆，动作读出在规则变化前后都保持逐位相同：

1. `fixed-internal`：感觉投影和内部循环均冻结；
2. `plastic-sensory`：只开放两个线索通道到 24 个单元的 48 个感觉权重；
3. `plastic-recurrent`：只开放固定稀疏循环中的 48 个预先选定权重；
4. `plastic-recurrent-no-homeostasis`：与上一项开放完全相同的权重，但关闭归一化。

每个控制器都分配 48 个内部可塑槽位；冻结控制把这些槽位置于关闭状态。感觉与循环可塑性不会同时开启。

循环网络每个单元有 6 条入边。两个开放入边分别来自对左、右线索最敏感的源单元，并承载主要初始循环强度；其余 4 条固定入边只提供弱背景耦合。这个结构先验对所有控制完全相同，并会作为当前结论的适用边界公开。

## 局部更新与内稳态

每个可塑突触保存一个回合内资格量。资格量由突触前活动和突触后单元的局部敏感度累积；选择发生后，由奖励优势和固定动作反馈共同调制。更新不读取正确分支标签，也不修改动作读出。

```text
eligibility_ij += pre_j × (1 - post_i²)
Δw_ij = η × reward_advantage × action_feedback_i × eligibility_ij
```

`plastic-sensory` 每次更新后把每个单元的两个线索权重归一到其反转前 L2 范数；`plastic-recurrent` 把每个单元开放的循环入边归一到反转前 L2 范数。关闭内稳态的控制只使用相同的权重裁剪。

这里的“局部”指突触资格只使用相邻的前/后活动；奖励和动作反馈仍是全局第三因子。因此 Gate F 不声称已经实现完全局部的生物学习规则。

## 报告与统计

- 原始规则训练后准确率；
- 反转与恢复阶段每个检查点的未见序列准确率；
- 达到 75% 正确率所需适应回合；
- 最终左右目标准确率与选择偏置；
- 动作读出、固定权重和开放权重的摘要，证明预算与冻结边界；
- 可塑权重 RMS 变化、最大绝对值、有限值比例和归一化漂移；
- 同模型种子配对差及 95% Student-t 置信区间；
- 至少一条规则反转和恢复的代表轨迹。

推断单位是模型种子，不能把同一模型的 200 个评估序列当作独立样本。

## 冻结验收

必须同时满足：

1. 四个控制器在变化前的动作读出、感觉权重、循环权重和状态完全相同；每类控制分配 48 个内部可塑槽位；
2. 变化前四者正确率均至少 90%；
3. 反转 800 回合后 `plastic-sensory` 正确率至少 75%，相对 `fixed-internal` 的同种子配对增益至少 20 个百分点且 95% CI 下界大于 0；
4. 反转 800 回合后 `plastic-recurrent` 正确率至少 70%，相对冻结控制增益至少 15 个百分点且 95% CI 下界大于 0；
5. 至少一个带内稳态的可塑控制在恢复原规则 800 回合后重新达到 75%；
6. 动作读出在两个适应阶段前后逐位不变；全部状态与权重有限；
7. 带内稳态控制的开放权重组 L2 范数平均相对漂移不超过 5%；无内稳态控制完整执行并单独报告，不能用它的结果替代关闭对照；
8. 相同配置与种子产生完全相同的报告和轨迹。

Gate F 通过只表示一种或两种内部可塑机制能在这个受控规则反转任务中承担持续适应，不表示已经实现开放世界学习、灾难性遗忘解决方案或生物真实突触可塑性。

## 正式运行前修订记录

第一个开发实现没有保留强制动作探索，并用快速移动的平均奖励作基线。规则刚反转时主体几乎总是失败，基线随即趋近 `-1`，学习信号消失；该结果被判定为探索协议错误，不作为机制证据。

第二个开发实现加入共享的 16% 探索并固定零奖励基线后，感觉投影能够适应，但两个随机开放循环入边在 6 条等强入边中无法改变整体表示；关闭归一化只能依靠权重无界膨胀获得部分适应。正式协议因此把两个开放入边冻结为左右线索敏感源，并让它们承载主要循环强度，其余入边作为弱背景耦合。该结构在正式种子运行前完成冻结；它限制了结论的普适性，但不会向任何控制泄露反转后的目标。
