# 下一阶段：稳定可学习性地图

> 状态：Map 0 已冻结并完成。具体协议见 [MAP_0.md](MAP_0.md)，正式负结果见 [experiments/map0-v0.1.md](experiments/map0-v0.1.md)。本文保留阶段设计背景。

## 研究问题

Gate A～F 每次固定一个问题并验证一个机制，但仍可能得到一组只对各自任务有效的控制器。下一阶段改问：**同一组底层参数能否在不逐任务调优的前提下，重复形成多种基础能力，并保持活动、权重和行为稳定？**

目标产物是一张功能形成相图，而不是一个最高分模型。

## 固定探针组

所有探针共享同一底层配置和预算，不允许针对单项任务单独调参数：

1. Gate B 派生的短期记忆；
2. Gate C 派生的延迟信用；
3. Gate F 派生的规则反转适应；
4. 新增的重复切换与旧规则保留；
5. 活动、连接或输入受到扰动后的恢复。

具身导航可以作为生态有效性检查，但第一轮不把稀疏导航噪声混入所有机制筛选。

## 第一轮参数空间

先扫描四个可解释且已有实现基础的轴：

- 循环增益或等价稳定性尺度；
- 内部可塑性速率；
- 内稳态强度；
- 探索率。

不做完整高维笛卡尔积。开发阶段使用 64～96 个空间填充配置和 8 个模型种子做粗测；只用这些结果选择边界区域，不发布确认性结论。正式阶段冻结区域和阈值后，使用未参与选择的至少 12 个模型种子复验边界、内部和失败区。

如果实现空间填充采样会引入额外依赖，可先使用仓库内确定性低差异序列；算法和种子必须写入版本化配置。

## 每个配置的记录

### 宏观能力

- 各探针的成功率和行为收益；
- 达到阈值所需样本数；
- 规则切换后的适应速度；
- 恢复旧规则时相对首次学习的速度；
- 未见线索编码、环境和任务变体上的迁移；
- 扰动后的性能恢复时间。

### 动力学与资源

- 非有限值、饱和、静默和群体同步比例；
- 活动均值、方差、稀疏度和有效维度；
- 小扰动造成的有限时域轨迹分离；
- 权重范数、漂移速度和触边比例；
- 事件数、CPU 时间和状态内存。

在没有严格估计前，不把有限时域扰动敏感度称为 Lyapunov 指数，也不据此宣称混沌。

## 分类与统计单位

每个“系统种子”是独立统计单位。先对每个种子按预注册阈值分类，再报告配置或区域的功能形成概率：

- `rigid`：稳定但不能达到学习阈值；
- `learnable-stable`：跨探针学习且状态、权重有界；
- `task-specialized`：只通过部分探针或依赖特定编码；
- `unstable`：发散、饱和、同步、持续漂移或严重遗忘。

二元形成概率使用 Wilson 区间；连续的同种子差异继续报告配对效应和置信区间。不能把回合数或时间步伪装成独立样本。

## 必要对照

对候选有效区域至少执行：

- 冻结内部可塑性；
- 置乱内部结构但保留连接数和权重分布；
- 关闭内稳态；
- 关闭探索并单独评估；
- 随机化后果信号；
- 使用未见线索编码和独立确认种子。

这些对照区分“系统形成能力”与随机特征可解码、外部奖励泄漏、持续探索碰巧命中或人工对齐通路。

## 实施批次

### Map 0A：测量与复现基础

1. 把四个扫描轴暴露到统一版本化配置；
2. 建立共享探针接口，禁止任务代码暗中覆盖底层参数；
3. 增加活动、权重、扰动和资源统计；
4. 固定开发种子、确认种子、阈值和失败原因格式；
5. 用当前 Gate B/C/F 配置做回归，确保新编排层不改变旧结论。

### Map 0B：粗粒度测绘

1. 生成确定性的空间填充配置；
2. 在固定探针组上批量运行开发种子；
3. 输出参数配置 × 能力 × 稳定性的长表；
4. 标记僵硬、候选有效、任务特化和不稳定区域；
5. Web 只展示相图、置信度和可下钻的代表运行，不以单条神经活动动画作为结论。

### Map 0C：边界复验与因果解释

1. 在未使用的确认种子上复验候选区域；
2. 对边界内外成对运行必要对照；
3. 寻找与宏观成功共同变化的中观统计量；
4. 对候选机制做消融，检验能否可预测地移动相图边界；
5. 发布成功区域或明确的负结果。

## 退出条件

进入 Gate G 前至少满足：

- 找到一个连续区域，而非单个幸运参数点；
- 区域在独立种子上具有非零且可重复的功能形成概率；
- 同一配置无需逐任务调参即可通过固定探针组的预定最低标准；
- 活动和权重有界，且不是全静默或全同步；
- 至少一项内部干预能可预测地降低形成概率或移动区域边界。

如果没有找到这样的区域，结果仍然完成本阶段：记录失败相图，定位僵硬、遗忘或发散边界，然后只修改一个局部机制重新测绘。不得用扩大网络规模规避失败；只有最小基座形成稳定区域后，规模才作为冻结参数下的独立变量进入实验。

## 预期产物

- 冻结后的 Map 0 实验规格；
- 统一实验编排模块和命令行入口；
- 版本化逐配置、逐种子结果；
- 稳定可学习性相图与区域置信度；
- 候选区域的因果干预报告；
- 是否进入 Gate G 的书面决策。

Map 0 已实现全部三批工作。协议完整通过，但独立确认没有找到连续稳定可学习区，因此暂不进入 Gate G；下一轮应只修改一个微观机制后重绘局部地图。
