# Map 0 冻结规格：稳定可学习性地图

> 状态：已完成。版本 `learnability-map/v0.1`，正式结果见 [experiments/map0-v0.1.md](experiments/map0-v0.1.md)，权威数据为 `app/public/map0-v0.1.json`。

## 问题

同一套 24 单元动态底层和局部更新规则，在不针对单项任务调参的情况下，是否存在一片跨系统种子可重复的参数区域，同时具备记忆、延迟信用、规则适应、重复切换、扰动恢复和数值稳定性？

Map 0 验收实验流程是否完整，与是否找到正区域分开判断。没有找到区域是有效负结果，不是协议失败。

## 统一底层

所有探针共享：

- 12 个感觉通道、24 个连续状态单元和 2 个动作；
- 每单元 6 条稀疏循环入边，其中 2 条允许局部可塑；
- 冻结动作读出后的奖励调制循环资格迹；
- 固定泄漏、资格衰减、动作读出学习率、温度和权重上限；
- 同一参数点只改变四个扫描轴，不由任务覆盖。

四个轴及正式范围：

| 轴 | 范围 |
|---|---:|
| 循环增益 | 0.08～0.92 |
| 内部可塑率 | 0.02～0.18 |
| 内稳态强度 | 0～1 |
| 探索率 | 0.04～0.24 |

使用无外部依赖的确定性 Halton 低差异序列生成 64 个配置，避免四维笛卡尔积。

## 固定探针组

动作读出先在原始线索规则上训练 320 回合，此后保持冻结。每个系统依次接受：

1. **短期记忆**：线索显示 2 步，关闭 8 步后选择；
2. **延迟信用**：反转规则，内部奖励更新额外衰减 6 步；
3. **规则反转**：从原规则切换到反规则，适应 240 回合；
4. **旧规则恢复**：切回原规则，动作读出仍冻结；
5. **重复反转**：再次进入反规则，同时比较第二次与第一次达到阈值的速度；
6. **扰动恢复**：按冻结动作读出的因果重要性永久失活前 25% 状态单元，先测即时损伤，再允许内部可塑性恢复。

每次评估使用 80 个平衡线索回合且关闭探索。阈值检查每 20 回合进行一次。

## 预注册判定

单个系统种子必须全部满足：

- 记忆、延迟信用和扰动后最终正确率分别不低于 65%；
- 反转、恢复和重复反转正确率分别不低于 70%；
- 第二次反转达到 70% 的回合数不多于第一次；
- 重要单元失活先造成至少 15 个百分点损伤，随后恢复至少 10 个百分点；
- 活动全部有限，饱和比例不高于 25%，同步比例不高于 50%；
- 开放循环权重组平均相对漂移不高于 100%。

分类规则：

- `learnable-stable`：六项探针和稳定性全部通过；
- `task-specialized`：稳定且通过至少两项探针；
- `rigid`：稳定但通过少于两项；
- `unstable`：有限性、饱和、同步或权重漂移越界。

参数点的形成概率以系统种子为统计单位并报告 Wilson 95% 区间。候选稳定区域要求至少 3 个相邻确认配置，每点形成概率不低于 50%。

## 种子与选择隔离

- 开发扫描：64 配置 × 8 个开发种子；
- 独立确认：选取开发最佳点、最近邻和其他高排名点，共 6 配置 × 12 个未参与选择的种子；
- 形成概率之外保留逐种子结果，回合或时间步不作为独立样本；
- 参数、种子和运行顺序全部进入版本化 JSON。

## 因果对照

对 6 个确认配置分别运行 12 个相同确认种子：

- 冻结内部可塑性；
- 置乱循环结构；
- 关闭内稳态；
- 关闭探索；
- 随机化后果信号。

形成概率相对基线下降至少 15 个百分点，或平均探针得分下降至少 10 个百分点，记为检测到内部因果变化。形成概率处于零地板时，探针得分仍可提供退化证据，但不能替代稳定区域成立。

## 验收

协议通过要求确定性采样、开发/确认种子隔离、全部运行完整、因果对照完整且输出有限。`stable_region_found` 单独报告，不参与协议完整性 `passed`。

正式结果没有找到稳定区域，因此 Map 0 完成，但不允许直接进入 Gate G。
