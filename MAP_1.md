# Map 1 冻结规格：双时间尺度内稳态局部重绘

Map 1 只回答一个问题：把 Map 0 的逐次参考范数投影替换成活动目标调节与慢速权重范数回拉，能否改善学习能力与参数稳定性的权衡？实验流程是否完整，与新机制是否有效分开判断。

## 唯一机制变更

保持 24 个内部单元、稀疏连接、动作读出、局部循环可塑性、六项探针、分类阈值以及 Map 0 的开发/确认随机世界不变。仅替换内稳态：

1. 每个单元维护绝对活动的指数滑动平均，目标活动为 `0.35`，EMA 率为 `0.05`；
2. 兴奋性增益以 `0.02` 的速率朝活动目标调节，并限制在 `[0.65, 1.35]`；
3. 开放循环权重组以 `0.03` 的速率在对数范数空间慢速回拉参考范数；
4. 旧参考范数机制作为同配置、同确认种子的配对对照，不参与候选选择。

Map 0 的精确范数投影分支保持原数值路径，旧结果可以独立回归。

## 冻结扫描

| 轴 | 范围 |
|---|---:|
| 循环增益 | 0.10–0.45 |
| 内部学习率 | 0.06–0.16 |
| 内稳态强度 | 0.25–1.00 |
| 探索率 | 0.04–0.18 |

- 开发：48 个确定性 Halton 配置 × 8 个 Map 0 开发种子，共 384 次；
- 确认：6 个开发候选 × 12 个 Map 0 确认种子，共 72 次；
- 旧机制配对：同 6 个配置、同 12 个确认种子，共 72 次；
- 因果对照：冻结可塑性、置乱结构、关闭内稳态、关闭探索、随机后果，共 360 次。

候选选择、Wilson 区间、区域邻接、形成概率和六项探针阈值全部沿用 Map 0。开发与确认种子严格隔离；确认结果不得反向修改机制参数或验收线。

## 预注册判断

新机制必须同时降低权重漂移，提高重复切换或损伤恢复，并产生至少三个相邻、形成概率达到 50% 的确认配置。因果干预必须产生预定义退化，全部状态和权重必须有限。只有满足连续稳定区域条件才允许讨论 Gate G。

## 正式结论

协议完整通过，但机制假设失败：384 个开发运行中 323 个不稳定；72 个确认运行中 59 个不稳定、12 个任务特化、1 个僵硬，没有形成实例。相同配置和种子下，新机制相对旧机制平均探针得分 `+1.2` 个百分点，但相对权重漂移 `+1.766`，重复反转平均 `-3.4` 个百分点，损伤恢复增益平均 `-1.4` 个百分点。

因此 Map 1 完成，但不进入 Gate G。详细结果见 [experiments/map1-v0.2.md](experiments/map1-v0.2.md)。
