# Map 2：最小动态基座审计

Map 2 不尝试复刻真实神经细胞，而是检验一个操作性动态基座能否同时满足：局部状态、有限连接改变、持续资源约束，以及不依赖人工状态重置的连续适应。三阶段严格串行；后一阶段只使用前一阶段冻结的机制。

## Map 2A：内生有界可塑性

冻结选择为**对称乘性软边界**。每条权重的正负更新都按其到相应边界的剩余空间缩放；边界由初始化参考组范数确定，不使用每步事后投影。除可塑性规则外，24 节点、结构、Map 1 探针、阈值和种子均不变。

正式结果包含 384 个开发运行、72 个独立确认、72 个旧加性机制配对和 360 个因果对照。相对加性更新，平均探针 `+0.9 pp`、重复反转 `+21.8 pp`、损伤恢复增益 `-8.4 pp`、相对权重漂移 `-0.687`。预注册的“降低漂移且重复反转或恢复不退化”成立，因此机制冻结；旧 Map 0 综合探针仍未形成稳定区域。

## Map 2B：持续基础供能

每个内部单元增加 `[0,1]` 局部资源。资源以固定速率持续恢复，维护、活动和实际权重改变持续消耗资源；资源不足统一调低活动幅度，既不携带任务信息，也不直接注入电位。没有睡眠阶段、休息状态机、预设节律或周期起搏。

正式结果包含 384 个开发运行、72 个确认、72 个关闭资源核算、72 个关闭供能和 360 个因果对照。持续供能的平均资源为 `0.892`，断供后为 `0.000`；平均探针从断供的 `37.7%` 恢复到 `51.6%`。相对关闭资源核算，探针 `+1.2 pp`、权重漂移 `+0.014`，保持在冻结容差内。资源机制通过并冻结，但原 Map 0 综合区域判据仍未成立。

## Map 2C：无重置连续流

冻结 2A、2B 后，单个系统连续运行 720 个试次。规则在至少间隔 30 个试次后以固定概率切换；内部活动、权重、资源和奖励基线均跨试次保留。单种子正式形成条件在开发扫描前冻结：

- 至少 5 次规则切换，且状态重置计数为 0；
- 总体准确率不低于 60%，切换后稳定期不低于 65%；
- 稳定期不差于刚切换窗口；
- 相对权重漂移不高于 1，平均资源不低于 0.05，所有输出有限。

48 个参数点各使用 8 个开发种子；6 个候选点各使用 12 个独立确认种子。相同点和种子还配对逐试次重置、关闭供能和冻结可塑性对照。

正式确认结果：

| 条件 | 总体准确率 |
|---|---:|
| 连续基线 | 66.1% |
| 逐试次状态重置 | 67.6% |
| 关闭供能 | 55.7% |
| 冻结内部可塑性 | 53.0% |

连续基线在刚切换窗口为 `43.8%`，稳定期恢复到 `72.9%`；平均相对权重漂移 `0.365`。参数 `0、16、20、21、29、45` 构成 6 点相邻稳定区域，满足至少 3 点、形成概率至少 50% 的冻结标准。逐试次重置只高 `1.4 pp`，没有解释连续表现；断供和冻结可塑性分别下降 `10.4 pp`、`13.1 pp`，达到因果必要性阈值。

## 结论边界

Map 2 支持一个有限结论：在当前二元规则任务族和 24 单元系统中，有界局部可塑性、非信息性持续供能与连续状态可以组成跨种子、跨相邻参数可复现的适应区。它没有证明通用学习、自然表征、开放式目标形成、类脑真实性或规模必然带来新能力。

Map 2 现冻结为 `reference-substrate/v1`：它是后续调整机制实验的参考载体，不声称功能完备或生物真实。下一步先按[动态调整机制计划](ADAPTIVE_MECHANISMS.md)测多规则保留边界；Scale 0 后移到调整机制形成跨任务稳定区域之后。不能把 Map 2C 的成功直接外推到视觉、开放环境或通用学习。

正式记录见 [experiments/map2-v0.5.md](experiments/map2-v0.5.md)，数据为 `app/public/map2a-v0.3.json`、`map2b-v0.4.json` 和 `map2c-v0.5.json`。
