# 第一阶段 MVP

## 1. 唯一研究问题

第一阶段只回答：

> 一个事件驱动的 LIF 稀疏循环网络，在明确的外部驱动条件下，能否产生可复现、不过度同步的持续活动；局部刺激是否会对后续状态造成可测量且可重复的改变？

“自维持”必须附带驱动条件描述，不能把起搏驱动、随机噪声和循环活动混为一谈。

## 2. 范围内

- 50～300 个 LIF 神经元；
- 兴奋性和抑制性神经元与突触；
- 固定的三维模型坐标；
- 稀疏循环连接；
- 路径长度、传播速度和突触延迟；
- 事件驱动模拟与解析漏电衰减；
- 可配置的起搏事件；
- 局部刺激；
- 神经元或突触消融；
- 固定种子、事件顺序和模型版本；
- 事件日志、检查点和确定性回放；
- 只读的基础 3D 游览；
- 放电栅格图、全网活动曲线和单神经元检查器。

## 3. 明确不做

第一阶段禁止加入：

- 图片输入、分类器和 AI 训练；
- STDP、强化学习或其他长期学习；
- 轴突和树突生长；
- 多区室神经元、离子通道和 Hodgkin–Huxley 模型；
- 动态记忆结论；
- GPU 神经模拟；
- 多脑区、VR 和写实细胞形态；
- 没有对应实验问题的插件系统或通用抽象。

这些内容保留在 [DESIGN.md](DESIGN.md) 的长期路线中，但不能成为 MVP 的依赖。

## 4. 实施关卡

### Gate 0：单神经元（已实现）

- 漏电衰减与解析公式一致；
- 兴奋和抑制输入能正确改变膜电位；
- 阈值、放电、复位和不应期行为明确；
- 边界时刻和同时输入有自动化测试；
- 相同输入事件产生相同放电事件。

通过前不实现网络和 3D 界面。

实现位于 [`src/lif.rs`](src/lif.rs)，验收测试位于 [`tests/gate0.rs`](tests/gate0.rs)。Gate 0 只承诺同一平台、同一工具链和同一配置下的逐事件确定性；跨 CPU 架构的浮点逐位一致仍是开放问题。

### Gate 1：小网络（已实现）

- 10～50 个神经元可以传播脉冲；
- 多个在途脉冲和循环连接工作正确；
- 同时事件具有固定处理规则；
- 不存在零延迟无限级联；
- 相同实验的事件日志摘要一致。

实现位于 [`src/network.rs`](src/network.rs)，验收测试位于 [`tests/gate1.rs`](tests/gate1.rs)。在途计数只包含已由神经元发出、尚未抵达的突触输入，不包含预先排定的外部事件。

### Gate 2：网络动力学（已实现）

- 扩展到 100～300 个神经元；
- 分别运行无驱动、起搏驱动和关闭起搏后的对照实验；
- 记录放电率、活跃比例、同步爆发、静默区间和周期性；
- 局部刺激前后轨迹差异可重复测量；
- 将经过试运行校准的验收区间冻结为实验版本的一部分。

具体实验见 [EXPERIMENT_001.md](EXPERIMENT_001.md)。在验收区间冻结前，不以“动画看起来复杂”判定通过。

指标与实验实现位于 [`src/metrics.rs`](src/metrics.rs) 和 [`src/experiment.rs`](src/experiment.rs)，验收测试位于 [`tests/gate2.rs`](tests/gate2.rs)。`experiment-001/v1` 的冻结配置与结果位于 [`experiments/experiment-001-v1.md`](experiments/experiment-001-v1.md)。

Gate 2 的当前结论是：网络在持续起搏支持下满足冻结的稳定活动区间，局部刺激会改变后续轨迹。持续起搏属于合法的正常运行模式；撤除后沉默只把当前配置分类为 `drive-dependent`，不是 Gate 2 失败。撤除实验保留为诊断工具。

### Gate 3：可视化回放（已完成）

- 读取已完成实验，而不是由渲染循环驱动模拟；
- 支持播放、暂停、倍速和时间轴跳转；
- 支持 3D 旋转、平移、缩放、选择和搜索神经元；
- 默认只显示活跃连接或选中神经元的局部连接；
- 支持查看在途脉冲、栅格图、活动曲线和单神经元状态。

Gate 3 不只是结果展示，而是决定后续研究方向的主要观察工具。界面必须让用户从全网异常活动逐步进入局部神经元、输入事件和传播路径，并能返回全局时间上下文。完整规格见 [VISUALIZATION.md](VISUALIZATION.md)。

Gate 3 的第一版完成条件：

- 能载入并播放 `experiment-001/v1` 的确定性结果；
- 3D 相机、选择、搜索和局部连接展开可用；
- 起搏源、兴奋/抑制连接、放电、不应期和在途脉冲具有稳定的视觉编码；
- 时间轴、3D 画面、栅格图和单神经元检查器指向同一模拟时间；
- 显示帧率和相机操作不改变模拟结果；
- 在 100～300 神经元基线下保持流畅交互，并保留实例化与可见性筛选路径；
- 可以仅通过界面定位一次放电的直接输入和来源连接。

实现位于 [`app`](app)、[`src-tauri`](src-tauri) 和 [`src/playback.rs`](src/playback.rs)，契约测试位于 [`tests/gate3.rs`](tests/gate3.rs)。`experiment-001/v1` 被导出为紧凑的三分支回放包：持续起搏对照、局部刺激和 1500 ms 撤除起搏。100 神经元浏览器验收中保持超过 60 FPS；正式性能结论仍需在目标 Mac 和 300 神经元数据上复测。

### Gate 4：干预分支

- 从检查点恢复；
- 在指定模拟时间注入刺激或执行消融；
- 创建新的实验时间线，保留原始结果；
- 并排比较原始时间线与干预分支。

Gate 4 通过后，第一阶段完成，但不再作为 [第二阶段模式刺激与功能群发现](PHASE_2.md)的整体验收前置。第二阶段先用确定性成对试验启动，再通过真实的刺激、同步比较和消融用例逐项补齐并验收 Gate 4；不预先建设暂时没有实验使用者的通用分支编辑器。

## 5. 停止条件

出现以下情况时暂停扩展功能，优先解决根因：

- 同一版本和种子的运行无法复现；
- 结果依赖显示帧率或机器运行速度；
- 无法解释一个参数使用的单位和作用位置；
- 稳态机制被用于掩盖基础网络持续沉默或持续爆发；
- 事件日志不能恢复或解释观察到的活动；
- 新功能没有对应的实验问题和验收指标。

## 6. 完成定义

第一阶段完成必须同时满足：

- Gate 0～4 全部通过；
- 无界面模式可以运行、保存和重放实验；
- 模型、参数、种子、输入和事件排序规则均被记录；
- 关键指标由程序生成；
- 文档明确区分工程现象与生物学结论；
- Windows 和 macOS 均能构建并播放同一份已保存实验。
