# 第二阶段：模式刺激与功能群发现

> 状态：下一阶段。无需等待 MVP Gate 4 的通用能力全部完成；第二阶段按实验需要逐步补齐确定性刺激、对照、消融和时间线比较，并以这些真实用例验收 Gate 4。

P2-0 最小成对试验已经实现，参数扫描与当前事实边界见 [experiments/phase2-p2-0.md](experiments/phase2-p2-0.md)。

## 1. 研究问题

第二阶段只回答一个问题：

> 一个持续运行且不过度同步的固定脉冲网络，在接受不同的 `3×3` 时空输入后，能否形成稳定、可区分，并对读出结果具有因果作用的分布式群体响应？

这里的“输出”不是要求网络在某个位置重新画出输入图形，也不是预先指定九个输出神经元。内部活动可以分散到任意神经元；输出由重复试验中的统计特征和外部读出器解释。

本阶段不宣称实现视觉理解、通用图片识别或真实脑区，只建立第一个完整的输入—响应—读出—干预实验闭环。

## 2. 核心假设

- **H0：无信息假设**。不同输入只造成随机波动，未见样本上的读出不优于机会水平；
- **H1：稳定响应**。相同输入在重复试验中产生比不同输入更相似的群体时间轨迹；
- **H2：可读出信息**。简单读出器能够在未参与拟合的试验上区分输入；
- **H3：功能群因果性**。移除由数据发现的候选响应群，会选择性破坏相应读出，而移除规模相同的随机神经元群不会产生同等影响。

H1、H2 和 H3 必须逐级成立。漂亮的传播动画、高放电率或空间聚集不能替代其中任何一级证据。

## 3. 系统边界

```text
3×3 Pattern
    ↓
Input Encoder       像素 → 有界外部电流或输入事件
    ↓
Frozen Network      固定拓扑、权重和神经元模型
    ↓
Response Recorder   事件日志 → 统一分析时间窗
    ↓
Analysis / Readout  功能群、原型距离、线性读出
    ↓
Intervention        候选群消融与匹配随机消融
```

输入编码器、网络和读出器必须分离：

- 输入标签只存在于实验元数据中，不沿突触传播；
- 读出器只读取网络状态，不向网络反馈；
- 输入神经元不参与读出，防止直接读取像素绕过循环网络；
- 固定网络实验完成前，不加入 STDP、训练权重或结构生长；
- 事件驱动模拟保持不变，统一 `1～10 ms` 分箱只属于分析层。

## 4. 输入定义

第一版使用九个独立感受器通道。像素值映射为有上限的外部电流或确定性输入脉冲率，让感受器仍然受阈值、漏电、不应期和抑制影响。禁止把神经元永久锁定为“正在放电”，否则无法区分外部输入与模型自身的动力学。

首批图形应尽量简单并控制总亮度：

- 空白输入，用于基线对照；
- 单像素输入，用于检查位置偏置；
- 中心点及其平移版本；
- 横线与竖线；
- 两条对角线；
- 像素数量相同、排列不同的配对图形。

“黑色代表激活”或“白色代表激活”都可以，但编码规则必须进入实验版本，不能由界面主题隐式决定。

试验时间暂定为：刺激前基线、持续刺激、刺激后恢复三个区间。具体时长、输入强度和重复次数通过参数扫描确定并冻结，不在实现前伪造生物学正确数值。初始工程试跑可以从每种图形 `30` 次、多个起搏相位和多个网络种子开始。

## 5. 第一关：持续输入下的稳定性

在寻找“响应神经元”之前，先比较空白、单像素和最强允许输入下的：

- 全网及兴奋/抑制神经元放电率；
- 活跃神经元比例；
- 同步爆发的次数、规模和持续时间；
- 静默区间与事件处理量；
- 输入结束后的恢复时间；
- 输入期间被不应期忽略的事件比例；
- 兴奋与抑制到达量的时间变化。

验收区间沿用 Experiment 001 的版本化方法：先公开扫描，再冻结沉默、稳定和持续爆发的工程边界。以下情况直接失败：

- 一小块持续输入使网络长期接近全体同步；
- 活动或统计结果依赖播放速度、帧率或界面是否打开；
- 有界输入造成无界事件增长或零时间事件循环；
- 稳定性完全依赖未记录的自动调参；
- 不同图形只因总输入强度不同而可被区分。

若固定 LIF 网络无法通过稳定性关卡，下一步只能逐项对照调整抑制比例、输入强度、连接权重或加入可关闭的适应机制，不能同时叠加多个稳态机制。

## 6. 第二关：响应记录与功能群发现

对每次试验，在排除九个输入通道后记录：

```text
TrialResponse
├── patternId / trialId / seed / pacemakerPhase
├── binnedSpikeCount[time][neuron]
├── sampledMembranePotential[time][neuron]
├── firstSpikeLatency[neuron]
├── refractoryFraction[neuron]
├── excitatory / inhibitory ArrivalCount[time][neuron]
└── populationMetrics[time][cohort]
```

统一采样是从精确事件日志重建的分析结果，不把模拟改成固定 tick。每个神经元的候选响应分数至少包含：

- 相对自身基线的变化；
- 相同图形多次试验的一致性；
- 对目标图形相对于其他图形的选择性；
- 第一次响应的延迟及其方差；
- 在不同起搏相位和轻微输入噪声下的稳定性。

随后按响应向量相似度、共同活动和响应延迟聚类，得到中性的候选功能群编号，例如 `F0`、`F1`。这些群体可以分散在 3D 空间中；空间相邻不是成员条件。只有通过读出和干预后，实验报告才可以赋予“横线选择群”等解释性名称。

## 7. 第三关：简单读出与因果干预

第一版只比较两种低容量读出器：

1. **响应原型距离**：计算每类训练试验的平均群体轨迹，将测试试验分配给最近原型；
2. **线性读出**：以分箱放电、膜电位摘要和延迟特征构成 `X`，使用 `score = W·X + b` 预测图形。

读出训练、阈值选择和功能群发现只能使用训练集。测试集必须在整个分析流程结束前保持不可见。复杂 CNN、Transformer 或多层网络不进入本阶段，因为它们可能在读出器中完成主要任务，掩盖主网络是否包含信息。

必须包含以下对照：

- 机会水平和标签置乱；
- 输入强度与亮像素数匹配；
- 多个起搏相位和运行种子；
- 多个网络结构种子；
- 保持度数统计的连接置乱；
- 消融候选功能群；
- 消融相同数量、相似放电率的随机神经元群；
- 从读出中排除输入通道及其直接复制特征。

只有候选群消融在重复试验中选择性降低对应图形的读出表现，并明显超过匹配随机消融，才能把相关性提升为初步因果证据。

## 8. 可视化要求

第二阶段界面服务于实验判断，而不是继续增加装饰性动画：

- `3×3` 输入面板：显示当前图形、编码强度和刺激区间；
- 网络状态平面：继续显示膜电位、放电和不应期；
- 响应差异层：显示“当前输入减去基线”或“两种输入的平均响应差”；
- 功能群着色：同一候选群在 2D 和 3D 中使用一致颜色；
- 试验一致性：显示均值、方差和单次试验，而不是只播放一次幸运样本；
- 读出面板：显示预测、原型距离或线性分数及测试集状态；
- 干预对比：控制、候选群消融和随机消融使用同步时间轴。

二维状态平面仍是神经状态地图，不是输入图片的内部重建。空间队列、候选功能群和预设实验模块必须使用不同视觉语义。

## 9. 工作包与顺序

### P2-0：最小纵向实验切片

- 暂不实现任意时刻、任意干预的通用分支编辑器；
- 使用确定性重跑，或从现有稳定检查点建立成对的空白/刺激试验；
- 成对试验共享网络结构、初始状态、起搏相位和运行种子，唯一差异是输入图形；
- 保存输入时间线、分支关系和结果摘要；
- 先跑通一个空白输入和一个 `3×3` 图形的生成、分析与回放。

这一切片已经开始验证 Gate 4，但不要求先完成全部 Gate 4。若从初始状态确定性重跑足以保证公平对照，第一版甚至可以不依赖任意检查点恢复。

### P2-1：输入与试验协议

- 定义 `3×3` 输入格式、编码器和单位；
- 批量生成空白、单像素和等亮度图形试验；
- 固定训练/测试划分、种子和起搏相位；
- 扫描并冻结持续输入的稳定区间。

### P2-2：响应数据与统计视图

- 从事件日志生成统一分箱特征；
- 实现基线差、试验方差和响应延迟；
- 增加 `3×3` 输入、响应差异和重复试验视图；
- 所有统计增加守恒、确定性和数据泄漏测试。

### P2-3：功能群与读出

- 实现中性功能群发现；
- 实现原型距离和线性读出；
- 运行标签置乱、连接置乱和跨种子对照；
- 只报告未见测试集结果及不确定性。

### P2-4：因果验证

- 按候选功能群补充神经元禁用或消融能力；
- 自动生成候选群消融和匹配随机消融分支；
- 比较读出变化、全网稳定性和非目标图形影响；
- 为每个功能标签保存指标、试验版本和干预证据链接。

### Gate 4 能力如何随第二阶段补齐

```text
P2-0 / P2-1  确定性重跑、刺激分支、分支元数据
P2-2         控制与刺激结果的同步比较
P2-4         神经元消融、匹配随机消融、因果分支
之后         只有真实实验需要时才补任意检查点和通用分支编辑
```

Gate 4 的完成由上述真实实验用例共同验收，而不是先建设一个暂时没有使用者的通用系统。

## 10. 阶段完成与停止条件

第二阶段完成至少需要：

- 持续模式输入不破坏冻结的稳定活动区间；
- 相同输入的响应在未见试验中可重复；
- 至少一种简单读出显著优于机会水平和标签置乱；
- 结果不是由输入总强度、输入神经元直读或单一起搏相位解释；
- 候选群消融产生超过匹配随机消融的选择性影响；
- 结果在多个运行种子成立，并明确报告跨结构种子是否成立；
- 模拟、分析、训练和界面四层均可独立复现。

遇到以下情况时停止扩展规模或学习机制：

- 网络尚未稳定就开始优化分类准确率；
- 训练集表现提高但未见试验不提高；
- 只有复杂读出器有效；
- 功能群随每次重复试验完全改变；
- 所谓功能群只是距离输入最近的神经元；
- 消融任何同规模神经元群都会造成相同结果；
- 当前 `100` 个神经元尚未形成证据闭环就扩大到数千节点。

如果 H1 或 H2 不成立，失败结果仍应保存。此时根据对照实验判断问题来自输入编码、网络稳定区间、拓扑或缺少可塑性，再只引入一个可关闭的新机制创建下一实验版本。

## 11. 输出物

- 版本化输入格式和试验清单；
- 稳定性参数扫描与冻结验收区间；
- 原始事件日志和分析特征数据；
- 训练/测试划分及防数据泄漏记录；
- 原型距离和线性读出结果；
- 功能群成员、选择性和可靠性报告；
- 控制、候选群消融和随机消融分支；
- 可回放的多试验可视化包；
- 包含失败、限制和未通过假设的实验报告。
