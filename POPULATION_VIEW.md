# 群体与区域观察规格

## 1. 问题

单次放电和在途脉冲是网络事件的最小观察单元，但它们本身不构成功能。类似数字电路中的单个电平，功能来自大量单元在结构和时间上的组合。对脉冲网络而言，需要同时观察：

- 一段时间窗口内哪些神经元共同活跃；
- 兴奋与抑制输入是否平衡；
- 活动在群体之间如何流动；
- 群体状态能否区分输入、预测结果或影响读出；
- 屏蔽群体后，行为是否发生可重复变化。

因此界面必须从单一的逐脉冲视图扩展为多尺度观察：

```text
区域间信息流
  ↓ 钻取
群体统计与内部结构
  ↓ 钻取
神经元、突触和原始事件
```

当前过渡原型已先实现左侧 3D 网络、右侧固定二维神经状态平面。二维平面从同一事件日志按当前显示时间重建膜电位、放电痕迹和不应期；它验证了群体状态可以形成时间序列，但尚未完成本规格后续的区域聚合与功能证据层。

## 2. 当前事实边界

`experiment-001/v1` 不能声称存在功能区：

- 神经元位置由固定种子在单位立方体内均匀随机生成；
- 除避免自连接和保证一条环形边外，突触目标随机选择；
- 播放契约没有区域或模块标识；
- 当前 10 ms 指标只汇总全网放电数和活跃神经元数。

第一版群体视图只能使用确定性的空间分箱，并标记为“空间队列”，不能命名为视觉区、记忆区、控制区等功能区。统计相关性也不能单独证明功能。

## 3. 三种实体

```text
Neuron             模拟动力学实体
SpatialCohort      观察层按空间规则形成的统计集合
FunctionalModule   实验定义并通过干预证据验证的模块
```

`SpatialCohort` 不写回模拟状态。`FunctionalModule` 的成员关系属于版本化实验元数据；除非实验明确使用模块化生成规则，否则它也不能暗中改变神经元方程。

## 4. 空间队列 v0

冻结基线采用 `2 × 2 × 2` 空间分箱，得到 `S0`～`S7` 八个队列。编号由 `(x >= 0.5, y >= 0.5, z >= 0.5)` 的三位组合确定，因而与帧率、事件顺序和遍历顺序无关。

每个 10 ms 滑动窗口计算：

```text
PopulationSample
├── cohortId
├── startMs / endMs
├── neuronCount
├── spikeCount
├── activeNeuronCount / activeFraction
├── excitatoryArrivalCount / Magnitude
├── inhibitoryArrivalCount / Magnitude
├── ignoredArrivalCount
├── pacemakerArrivalCount
├── meanThresholdMarginMv
└── synchronousFraction
```

区域间传播使用流量矩阵：

```text
PopulationFlow
├── sourceCohortId / targetCohortId
├── startMs / endMs
├── pulseCount
├── excitatoryCount / inhibitoryCount
├── totalMagnitudeMv
├── ignoredAtTargetCount
└── meanDelayMs
```

同一区域内部的流量也必须保留，不能只画跨区域边。

## 5. 视觉语义

### 全局群体视图

- 区域体积：神经元数量，只随拓扑变化；
- 区域填充亮度：窗口内活跃比例；
- 区域边框脉动：同步程度；
- 区域间流线粗细：窗口内脉冲数量；
- 流线颜色：兴奋/抑制的净占比；
- 流线动画：方向，不编码模拟速度；
- 灰色警示：大量输入在不应期被忽略。

默认不显示所有原始脉冲。点击区域后展开其内部神经元；点击区域间流线后，才显示组成该聚合流的原始事件。

### 统计面板

群体面板必须显示绝对值和归一化值。例如 `spikeCount` 与每神经元放电率同时显示，避免大区域天然显得更活跃。时间窗口必须始终可见，不能让用户误以为统计值是瞬时状态。

## 6. 功能声明的证据门槛

区域只有满足至少一种操作性证据后才能获得功能标签：

1. **输入区分**：不同输入产生可重复且可交叉验证的群体状态差异；
2. **预测或读出**：区域状态能预测后续输入、网络状态或外部读出；
3. **干预因果**：屏蔽、消融或扰动区域会稳定改变目标结果；
4. **鲁棒性**：结论在多个种子、轻微参数扰动和输入噪声下成立；
5. **特异性**：不是所有区域都同样预测或影响同一结果。

仅有高放电率、同步、空间聚集或漂亮的传播动画，均不足以证明功能。

## 7. 模块化实验 v1

在空间队列原型完成后创建独立实验版本，不修改 `experiment-001/v1`：

```text
Module A ──→ Module B ──→ Module D
   │             ↑            │
   └────→ Module C ────────────┘
              抑制门控
```

第一版只使用中性名称 `A`～`D`。生成器显式配置：

- 每个模块的神经元数量与兴奋/抑制比例；
- 模块内连接概率或固定出度；
- 模块间连接矩阵；
- 输入目标模块和读出模块；
- 固定种子、权重、延迟和起搏条件。

至少运行控制、定向刺激、模块屏蔽和跨种子复现实验。只有结果通过第 6 节证据门槛，才允许在实验报告中增加功能解释。

## 8. 数据与性能边界

区域统计属于分析和播放层，不进入渲染循环的模拟事实。实现使用滚动窗口计数器：事件进入窗口时累加，离开窗口时扣除。每帧成本目标为：

```text
O(本帧进入/离开窗口的事件数 + 区域间可见流数)
```

禁止每帧重新扫描完整事件历史。区域数第一版限制为不超过 16；流量矩阵最多 256 项，远小于逐突触渲染成本。

## 9. 验收

### 空间队列原型

- 相同数据和时间产生完全相同的队列成员与统计；
- 八个队列的成员并集等于全部神经元，且互不重叠；
- 队列放电、到达和流量求和等于同窗口的全网原始事件数；
- 切换群体/微观视图不改变模拟时间和事件摘要；
- 界面明确显示“空间统计，不代表功能区”。

### 模块化实验

- 模块元数据进入实验版本和播放契约；
- 模块内外连接数量符合冻结配置；
- 控制、刺激和屏蔽分支可确定性复现；
- 功能标签附带对应指标、干预分支和跨种子证据链接；
- 未达到证据门槛时只显示中性模块名称。

## 10. 实施顺序

1. 前端从现有播放数据计算八个空间队列及滚动统计；
2. 增加群体总览、区域流量和向微观视图钻取；
3. 将同一算法移入 Rust 分析层并增加守恒测试；
4. 扩展播放契约以携带显式模块元数据；
5. 新建模块化实验版本、刺激和屏蔽分支；
6. 使用操作性证据决定是否赋予功能解释。
