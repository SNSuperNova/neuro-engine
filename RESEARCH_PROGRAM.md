# 研究纲领：宏观功能形成与稳定可学习性

> 状态：当前总研究方向。具身环境是主要实验探针，LIF、连续状态和局部可塑性是可替换的底层机制，不是项目本身的定义。

## 一句话假设

在有界非线性动态系统中，可能存在一片可重复的参数区域，使局部可塑性、内稳态和探索共同产生跨任务的稳定适应；我们尝试通过宏观输入输出实验与内部因果干预，测出这片区域并反推它需要的微观机制。

这不是“先复制一个大脑，再等待智能出现”，也不是只为一个任务优化分数。当前目标是研究：**宏观功能在什么条件下形成、保持、迁移或崩溃。**

## 研究对象

系统可以理解为一种持续运行的动态计算介质：

- 单元动力学类似局部指令集；
- 连接结构类似程序结构；
- 权重既是参数，也是可改变的程序部分；
- 瞬时活动是运行时状态；
- 群体状态轨迹是一次计算过程；
- 可塑性负责根据经历修改系统；
- 内稳态约束资源、尺度和活动范围；
- 环境提供输入、后果与反馈。

普通计算机中程序、数据和运行状态通常可以清楚分开；这里三者相互耦合。单个节点不必拥有完整“算法”，更准确的说法是它形成一个局部计算角色，而宏观能力分布在节点、连接、状态轨迹、可塑性与环境闭环中。本文把它称为**分布式动力程序**，而不是巨大阈值表或许多独立算法的简单相加。

## 三个观察层次

| 层次 | 研究内容 | 典型问题 |
|---|---|---|
| 微观 | 单元状态、连接、衰减、可塑性、内稳态 | 去掉某条局部规则后还会学习吗？ |
| 中观 | 活动群、回路、吸引域、信息路径和有效维度 | 成功系统是否共享某些组织性质？ |
| 宏观 | 记忆、适应、保留、迁移、恢复和行为收益 | 系统是否在未见条件下形成可复现能力？ |

项目主要研究宏观功能如何从微观规则中形成，但不能只看最终分数。中观统计连接“做到了什么”和“为什么做到”，内部干预则提供因果证据。

## 灰盒实验立场

我们知道并控制微观更新规则，但不预先指定每个节点承担什么语义。因此系统既不是完全未知的黑盒，也不是逐功能手工布线的白盒。

实验采用两条互补路径：

1. **外层黑盒测绘**：改变动力学、可塑性、内稳态和环境分布，估计宏观功能形成概率；
2. **内层因果解释**：冻结、置乱、消融或局部关闭机制，检验哪些内部条件对结果必要。

最终关心的对象不是某一次漂亮轨迹，而是：

```text
P(功能形成 | 动力学参数、可塑性规则、内稳态强度、探索和环境分布)
```

## 自由度与不可退让的约束

不同随机种子不必得到相同节点编号、权重矩阵或逐步轨迹。多个微观结构 A、B、C 可以实现同一个宏观功能，这正是系统自由度的一部分。

但“自然形成”不能成为无法证伪的说法。正式结果必须满足：

- 功能由外部任务和行为指标预先定义；
- 在独立种子上给出形成概率，而不是展示幸运个例；
- 优于冻结、随机、置乱或关闭关键机制的对照；
- 能处理未见输入，不能只记忆训练样本；
- 内部干预造成可预测的性能变化；
- 成功系统在中观层面出现可重复的统计性质；
- 外部读出器不得单独从随机复杂活动中“发现”未经主体使用的意义。

因此我们不要求微观同构，但要求**统计上的宏观等价与可重复的因果边界**。

## 与传统机器学习的关系

传统机器学习同样可以是动态的、分布式的和难以解释的。本项目的独特性不在于“有神经元状态”或“不使用向量”，而在研究重心：

- 常见机器学习先定义架构、目标函数和优化器，再提高指定任务表现；
- 本项目固定一组任务探针，寻找持续运行的自适应底层在哪些区域同时具备稳定、学习、保留和迁移能力；
- 训练与推理不必彻底分离，状态、行为、可塑性和内稳态可以在运行中共同变化；
- 不以某一种神经元模型为先验答案，而以机制消融和跨任务证据决定其是否保留。

这仍然是在设计算法：局部更新规则、资源约束和环境反馈都由我们定义。不同之处是宏观策略不被逐条写出，而由系统在这些边界内形成。

## 功能形成的操作定义

某项能力只有同时满足以下证据，才称为“形成”：

1. **行为成立**：达到冻结前声明的任务阈值；
2. **学习成立**：相对关闭可塑性有稳定增益；
3. **泛化成立**：增益延伸到未见输入、环境或上下文；
4. **重复成立**：多个独立系统种子具有非偶然的形成概率；
5. **因果成立**：置乱、消融或关闭关键机制后按预期退化；
6. **持续成立**：能力不会立即被下一次学习覆盖，且状态与权重保持有界。

一次规则反转后的重新学习只证明适应，不自动证明记忆保留；可视化中的复杂活动只证明复杂，不自动证明功能；一个强外部分类器能解码状态，也不自动证明系统自己使用了这份信息。

## 稳定可学习性地图

假设中的参数空间预计至少包含四类区域：

| 区域 | 典型表现 |
|---|---|
| 僵硬/静默 | 状态迅速消失或无法改变，稳定但学不会 |
| 稳定可学习 | 记忆、适应、恢复与参数有界同时存在 |
| 任务特化 | 单个任务很强，但更换任务或编码就失效 |
| 不稳定/饱和 | 活动同步、数值饱和、权重漂移或持续遗忘 |

下一阶段不追逐一个最高分参数点，而要找出是否存在连续、可重复的“稳定可学习区”。需要同时观察活动有限性、同步/静默、扰动敏感度、记忆容量、有效维度、样本效率、遗忘、重复切换速度、跨种子迁移以及权重和能量稳定性。

“混沌”目前不是既定结论。除非得到严格的扰动增长或等价测量，项目只称当前底层为有界非线性动态系统。

## 当前证据边界

Gate A～F 已分别建立奖励审计、短期状态、延迟信用、循环结构、LIF 对照和冻结读出后的内部可塑性证据。这说明当前系统可以在精心控制的任务中形成若干局部能力。

Map 0 已进一步完成跨任务稳定性测绘。独立确认没有找到连续稳定可学习区，多数系统只能通过部分探针。这支持“当前瓶颈首先是机制组合，而不是单纯节点数量”的工作判断，但不证明扩大规模永远无效。

Map 1 又以同配置、同种子配对测试了双时间尺度内稳态。它略微提高平均探针得分，却显著增加权重漂移，并使多数确认运行转为不稳定。这把工作判断进一步收窄：当前加性内部可塑性与事后内稳态存在冲突，优先级高于节点扩容。

Map 2 依次引入内生软边界、持续非信息性资源和无状态重置连续流。2A/2B 分别通过漂移与资源因果判据；2C 在 720 试次连续变化任务中确认 6 个相邻参数点，连续基线 `66.1%`，断供 `55.7%`，冻结可塑性 `53.0%`。这说明当前基座在这一任务族内存在统计可复现适应区，但没有推翻 Map 0 关于多探针通用稳定性的负结果。

项目也不假定能够先搭出一个“真实神经元基座”。真实细胞的层次没有明确完备边界，微观细节更多也不自动带来可验证性。[最小动态基座审计](FOUNDATION_AUDIT.md)现已完成；下一步只改变节点数量，测试当前适应区域对规模是否稳健。

它还没有证明：

- 同一底层无需逐任务调参就能稳定学习多种能力；
- 规则恢复来自保留，而不是再次从头学习；
- 内稳态提高了行为稳定性，而不只是控制权重范数；
- 能力形成来自宽参数区域，而不是狭窄的人工配置；
- 当前系统处于混沌边缘或接近真实脑组织；
- 扩大节点数量会自然带来更高级功能。

Gate F 仍依赖二元规则、16% 强制探索、全局后果信号和预对齐可塑通路。它是进入宏观功能形成研究的起点，不是终点。

## 研究顺序

1. 用固定探针组绘制[稳定可学习性地图](LEARNABILITY_MAP.md)；
2. 判断是否存在跨种子、跨任务的连续有效区域；
3. 通过关闭内稳态、探索、可塑性及结构置乱解释区域边界；
4. 若有效区域存在，再进入多规则保留、迁移和更自然的输入编码；
5. 若不存在，先测试可塑性是否内生有界，再单独加入持续、无信息的基础供能；
6. 不预设睡眠、休息或节律，让静默、爆发和轮换成为自然演变的观测结果；
7. 在持续变化输入中验证机制不依赖人工重置；
8. 只有小规模机制形成连续稳定区域后，才冻结参数扫描节点数量。

项目允许负结果。测出“什么条件下功能不会形成”与测出成功区域同样重要。
