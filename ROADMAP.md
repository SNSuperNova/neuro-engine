# 下一阶段路线图

> 原则：先证明一个机制解决了已定义的问题，再增加下一项。版本号表示实验问题，不表示规模升级。

## 当前基线：Embodied v1

- [x] 二维环境、能量、食物和危险；
- [x] 有限感觉与四种动作；
- [x] 带泄漏状态和适应的内部控制器；
- [x] 奖励调制资格迹；
- [x] 训练/测试地图隔离；
- [x] 关闭学习、权重置乱和内部消融；
- [x] 行为仪表盘和确定性发布数据。
- [x] Gate A 奖励分解、感觉干预和多种子辅助审计。

## 推荐顺序概览

```text
v1 当前基线
  ↓
Gate A 奖励与感觉审计 ✓
  ↓
Gate B 状态必要性 ✓
  ↓
Gate B 鲁棒性边界 ✓
  ↓
Gate C 时间信用分配 ✓
  ↓
Gate D 内部循环动力学 ✓
  ↓
Gate E 连续状态与脉冲状态等预算比较 ✓
  ↓
Gate F 可塑内部网络与环境变化
```

Gate A～E 已完成。下一步进入内部可塑性与环境变化，不增加复杂细胞类型或网络规模。

## Gate A：排除任务捷径（已完成）

### 问题

当前导航使用精确的最近食物相对方向和“接近食物”的小额塑形奖励。它们让闭环容易验证，但也可能替主体完成了最困难的目标发现和空间搜索。

### 实验

1. 扫描距离塑形系数：`0.035 → 0.02 → 0.01 → 0`；
2. 分别降低食物方向精度、加入感觉噪声和随机遮挡；
3. 保持地图、训练预算、初始化和评估种子一致；
4. 同时报告食物数、完成率、总能量、碰撞、训练方差和达到阈值所需回合；
5. 保留关闭学习与无方向信息对照。

### 验收

- 明确性能来自哪些人为信息；
- 至少在一个不含距离塑形的配置中显著胜过关闭学习；
- 如果失败，将结果记为能力边界，不用增加网络规模掩盖问题。

结果：无距离塑形时，学习相对关闭学习的平均食物增益为 `5.218`，95% CI `[4.544, 5.891]`；完全移除食物方向后只剩 `0.676 [0.255, 1.097]`。距离塑形不是必要条件，但精确方向感觉是主要人工辅助。详见 [experiments/gate-a-v1.1.md](experiments/gate-a-v1.1.md)。

## Gate B：证明内部状态有用（已完成）

### 问题

当前内部单元具有泄漏状态，但环境提供的即时方向信息可能让无记忆策略同样完成任务。内部活动被消融后变差，只说明读出依赖这些特征，不等于证明时间状态有用。

### 实验

比较参数量和训练预算匹配的三种控制器：

1. `stateless`：每步只使用当前感觉；
2. `leaky-state`：当前连续状态控制器；
3. `state-reset`：结构相同，但每一步清空历史状态。

冻结任务为 [GATE_B.md](GATE_B.md) 中的 `delayed-cue-fork/v1`：早期线索在岔路决策前永久消失，正确动作只能由历史确定。用历史置乱、零信息线索和线索重新可见对照排除任务泄漏。

### 验收

- 在完全可观察任务中三者差距可解释；
- 在部分可观察任务中 `leaky-state` 稳定优于两个无记忆对照；
- 优势在未见地图和多个随机种子上成立。

结果：延迟线索下 `leaky-state` 正确率为 `81.8% [73.6%, 90.0%]`，相对 `state-reset` 的同种子配对增益为 `30.6 [22.6, 38.6]` 个百分点；历史置乱后降至 `48.8%`。全部冻结验收通过，详见 [experiments/gate-b-v1.2.md](experiments/gate-b-v1.2.md)。

## Gate B 鲁棒性边界（已完成）

不修改 Gate B 结论，按 [GATE_B_ROBUSTNESS.md](GATE_B_ROBUSTNESS.md) 单因素扫描无信息延迟、线索投影强度和持续感觉干扰，公开当前固定状态机制的有效范围。

结果：可靠记忆边界为 4 步；6 步正确率降至 `60.0% [54.1%, 65.9%]`，8 步接近机会水平。持续输入尺度增至 0.40 时还出现显著分支偏置。详见 [experiments/gate-b-robustness-v1.2b.md](experiments/gate-b-robustness-v1.2b.md)。

## Gate C：证明资格迹解决延迟奖励（已完成）

### 问题

当前奖励大多紧邻动作，尚不能证明资格迹真的完成了跨时间责任分配。

### 实验

1. 比较 `eligibility_decay = 0 / 0.50 / 0.88 / 0.97`；
2. 设置真实能量延迟 `0 / 2 / 4 / 8` 步；
3. 使用当前可见线索和无状态控制器，移除记忆混杂；
4. 使用强制等待段，避免等待期动作改变任务；
5. 加入线索与目标独立的防泄漏对照。

### 验收

- 无资格迹时随奖励延迟显著退化；
- 合理资格迹能恢复部分性能；
- 给出有效时间窗口，而不是只选择最好的一次运行。

结果：延迟 8 步时，资格迹 0.88 为 `99.3% [98.7%, 99.9%]`，无资格迹为 `47.0% [41.3%, 52.6%]`；同种子配对增益 `52.3 [46.5, 58.1]` 个百分点。随机线索对照为 `49.8% [48.5%, 51.0%]`。全部冻结验收通过，详见 [experiments/gate-c-v1.3.md](experiments/gate-c-v1.3.md)。

## Gate D：加入真正的内部循环动力学（已完成）

### 问题

v1 的内部单元只有自身泄漏和适应，没有隐藏单元间连接。它更接近有状态随机特征层，而不是 Echo State Network、Liquid State Machine 或神经微回路。

### 实验

在不改变感觉和动作接口的情况下，增加可关闭的稀疏固定内部连接：

- 明确兴奋/抑制符号；
- 控制谱半径或等价稳定性参数；
- 保持参数量、状态数和训练预算匹配；
- 对照 `no-recurrence`、`shuffled-recurrence`、`recurrent`；
- 测量记忆长度、状态饱和、行为收益和计算成本。

### 验收

- 循环连接在 Gate B 的记忆任务上提供可重复收益，或明确记录没有收益；
- 内部活动不会塌缩到全同步、全静默或数值发散；
- 收益不能只由更多参数解释。

结果：延迟 8 时结构化循环为 `80.6% [70.4%, 90.9%]`，相同权重集合与谱的置乱循环为 `56.2% [49.7%, 62.6%]`；配对增益 `24.5 [14.3, 34.6]` 个百分点。可靠记忆边界从 4 步延长到 8 步，且活动通过有限值、饱和、静默和同步冻结检查。详见 [experiments/gate-d-v1.4.md](experiments/gate-d-v1.4.md)。

## Gate E：连续状态与脉冲状态等预算比较（已完成）

前四个 Gate 已建立稳定基准。Gate E 接入保留的 LIF 事件网络，比较：

1. 无状态策略；
2. 连续泄漏状态；
3. 相同可训练连接预算的 LIF 脉冲网络。

统一感觉信息、动作接口、训练回合、可训练参数数量和随机种子。报告行为、样本效率、运行时间、事件数、内存和鲁棒性。

验收不是“脉冲画面更像大脑”，而是它至少改善记忆、稀疏计算、延迟信用分配或损伤鲁棒性之一。若没有，则保留负结果，不把脉冲网络升级为默认实现。

结果：在连续泄漏与脉冲迹衰减同为 0.86、动态标量和可训练连接预算匹配时，延迟 8 的 LIF 为 `88.5% [85.3%, 91.6%]`，连续状态为 `67.3% [60.5%, 74.1%]`，配对增益 `21.2 [13.6, 28.7]` 个百分点；可靠记忆边界分别为 12 和 4 步。LIF 只在记忆轴通过：当前 CPU 实现慢约 6.3 倍，概念状态载荷相同但软件实现状态更大，25% 损伤下降也更大。因此保留为实验分支，不升级为默认实现。详见 [experiments/gate-e-v1.5.md](experiments/gate-e-v1.5.md)。

## Gate F：内部可塑性与持续适应

完成前述因果基线后，一次只加入一项：

1. 感觉到内部单元的可塑性；
2. 内部循环连接的局部可塑性；
3. 内稳态与突触归一化；
4. 局部而非全局奖励调制；
5. 短时突触疲劳；
6. 环境规则反转、感觉损伤和重新学习；
7. 多个相互竞争的生理需要。

每项机制必须改善未见环境行为、样本效率、适应速度、能耗或鲁棒性之一，并有独立关闭对照。

## 已完成批次：Gate E

本批只覆盖 Gate E，没有同时实现 Gate F：

1. 冻结连续状态与 LIF 的预算、时间尺度、输入编码和动作读出；
2. 对照无状态、连续状态和 LIF 脉冲状态；
3. 扫描 4 / 8 / 12 步记忆边界，并执行 25% 单元损伤；
4. 报告行为、样本效率、事件、运行时间和概念状态内存；
5. 发布 `embodied-learning/v1.5-lif-comparison`。

## 下一实现批次

只实现 Gate F：先冻结环境规则反转或感觉损伤任务，再一次只开放一种内部可塑性。首选比较固定内部网络、可塑感觉投影和局部可塑循环三者，必须包含内稳态或归一化关闭对照，不能同时加入多种生物机制。

## 暂不进入

- 数千到数十亿神经元；
- 意识或人类认知宣称；
- 无任务的大规模自发活动；
- 同时加入多种生物机制；
- 多树突、复杂分子通路和精细形态；
- 用复杂外部模型替主体完成任务。
