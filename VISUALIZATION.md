# 可视化优先规格（Gate 3 已完成）

## 1. 定位

Gate 3 的核心交付不是增加更多神经元机制，而是建立一套可以在空间和时间中检查网络的“神经显微镜”。

可视化的主要作用：

- 发现活动集中、传播停滞、全网爆发和异常同步；
- 观察起搏活动如何进入循环网络；
- 判断兴奋与抑制在空间上的覆盖方式；
- 追踪一次放电的直接输入和后续传播；
- 比较局部刺激前后的活动轨迹；
- 为下一轮连接、延迟、能量或模型设计提供依据。

视觉观察用于提出和筛选假设；统计指标继续负责验证现象，干预实验负责判断因果关系。

## 2. 第一版界面

```text
┌────────────┬──────────────────────────┬────────────────┐
│ 实验与图层 │                          │ 神经元检查器   │
│            │       3D 神经空间        │                │
│ 播放模式   │                          │ 膜电位/阈值    │
│ 连接过滤   │                          │ 不应期         │
│ 起搏显示   │                          │ 输入/输出连接  │
│ 刺激对比   │                          │ 事件来源       │
├────────────┴──────────────────────────┴────────────────┤
│ 时间轴 / 放电栅格图 / 活动曲线 / 当前模拟时间         │
└───────────────────────────────────────────────────────┘
```

桌面容器使用 Tauri，界面使用 TypeScript，3D 使用 Three.js + WebGL2。Rust 内核保持无界面，不引用 UI 类型。

## 3. 空间游览

必须同时支持：

1. 自由观察：旋转、平移和缩放；
2. 目标导航：按神经元 ID 搜索并自动聚焦；
3. 连接追踪：沿输入或输出突触跳转；
4. 局部隔离：只显示选中神经元的一至三阶邻域；
5. 活动追踪：相机跟随一次脉冲或一组传播事件；
6. 返回路径：保留观察历史，避免进入网络后迷失。

需要一个小型空间概览或方向指示。相机和显示布局不能修改模型坐标。

## 4. 视觉编码

### 神经元

```text
基础颜色        兴奋性/抑制性
外圈标记        起搏源
亮度            膜电位接近阈值的程度
瞬时闪光        放电
暗色或冷却环    绝对不应期
描边            选中、搜索或跟踪状态
尺寸变化        默认不用来编码膜电位，避免遮挡和误读
```

起搏器是模拟内持续存在的节律实体，不显示成界面之外的后台线程。当前实现是明确记录的周期事件源；未来加入自主起搏神经元后，可以显示其能量、内在周期和疲劳状态。

### 连接

```text
颜色            兴奋/抑制
粗细            突触权重
透明度          近期活动或选择相关性
曲线长度        模型空间路径
紫色亮点与拖尾  在途脉冲
```

Gate 3 的百神经元场景默认常驻显示全部连接，以便先看清网络拓扑；基础连接使用低透明度，避免压过节点和信号。以下连接叠加高亮：

- 正在传播脉冲的连接；
- 选中神经元的输入和输出；
- 指定空间半径或跳数内的连接；
- 超过权重或近期活动阈值的连接；
- 用户锁定的追踪路径。

信号使用与兴奋/抑制配色分离的紫色核心、光晕和短拖尾。到达后的短暂余辉只属于播放器的视觉表达，不写回模拟状态，也不改变事件时间。

## 5. 时间游览

显示时间与模拟时间完全分离：

```text
simulationTime   事件发生的权威时间
displayTime      当前查看的模拟时间
displayFrame     界面刷新节奏
```

第一版支持：

- 播放、暂停、倍速和拖动；
- 跳到上一个或下一个放电事件；
- 跳到选中神经元的上一次输入或放电；
- 以任意显示帧率插值在途脉冲位置；
- 时间轴、3D 场景、栅格图和检查器同步；
- 明确显示当前查看的是完整运行还是干预分支。

在途脉冲位置：

```text
progress = (displayTime - sendTime) / (arrivalTime - sendTime)
```

播放器不生成新的模拟事件，也不能改变事件顺序。

## 6. 起搏与能量的视觉位置

持续起搏是正常运行条件，不需要默认关闭。界面必须能区分：

- 起搏器直接引起的输入；
- 起搏后通过突触传播的活动；
- 外部刺激；
- 未来可能加入的噪声或稳态机制。

撤除起搏作为可选诊断操作，结果分类为：

```text
drive-dependent       撤除后快速沉默
transiently-persistent 撤除后维持有限时间
autonomously-persistent 撤除后长期维持
```

能量模型不在 Gate 3 实现，但界面数据结构应允许以后增加：当前能量、补充速率、放电能耗、低能量警告和因能量不足跳过的放电。

## 7. 数据接口

前端不接收逐事件 JSON，也不持有权威模拟状态。Rust 按时间块提供：

```text
PlaybackChunk
├── timeRange
├── spikeEvents[]
├── arrivalEvents[]
├── neuronSamples[]
├── metricSamples[]
└── inFlightIntervals[]
```

第一版可以直接从内存中的 `NetworkRun` 生成播放块。文件格式和检查点属于后续存储工作，不应阻塞可视化原型。

事件批次使用紧凑二进制或 typed array。控制命令和低频元数据可以使用 JSON。

## 8. 性能预算

优先目标：

```text
基线规模             100～300 神经元
基线连接             1,000～10,000
目标交互帧率         60 FPS
单帧 CPU UI 预算      约 4 ms
单帧 GPU 预算         约 12 ms
事件预取窗口          可配置，默认覆盖数个显示帧
```

扩展目标是在 10,000 个神经元时仍能以局部视图保持至少 30 FPS，而不是强制绘制全网所有连接。

从第一版开始采用：

- GPU 实例化绘制神经元；
- 合并的连接几何或线段缓冲区；
- 视锥、距离、活动和选择过滤；
- 只更新发生变化的 GPU 数据；
- 3D、曲线和事件解码分开计时；
- 开发模式显示 FPS、帧时间、可见节点、可见连接和事件吞吐。

## 9. Gate 3 验收场景

### 场景 A：全局活动

载入 `experiment-001/v1`，从全局视图确认起搏源、活动扩散、抑制参与和两个同步爆发段可以被定位。

### 场景 B：单神经元追踪

选择一个放电神经元，暂停到放电前，查看直接输入、膜电位、阈值和不应期，再沿输出连接追踪后续事件。

### 场景 C：刺激对比

并排或切换显示控制分支与局部刺激分支，定位首次分歧时间和受影响空间区域。

### 场景 D：起搏依赖诊断

比较持续起搏运行与撤除实验。界面应明确显示“正常运行条件不同”，不能把撤除后的沉默自动标记成故障。

### 场景 E：性能

在 300 神经元基线上播放完整实验，确认相机操作和时间轴不会改变事件摘要；记录帧率、可见元素和内存占用。

## 10. 已完成的实施顺序

1. 已定义 Rust → 前端的只读播放数据契约；
2. 已建立 Tauri + TypeScript + Three.js 应用骨架；
3. 已使用 GPU 实例化渲染神经元，并按选择显示连接；
4. 已加入 Z-up 的 `WASD`、`Q/E`、鼠标相机，及选择、搜索、历史和检查器；
5. 已加入时间轴、放电和在途脉冲；
6. 已加入栅格图和活动曲线；
7. 已加入持续起搏对照、刺激分支与撤除诊断切换，并显示刺激首次分歧；
8. 已建立 FPS、可见节点、连接和脉冲面板并完成 100 神经元场景验收。

Gate 4 前仍不优先加入能量、学习、生长或更复杂神经元模型。

## 11. 当前实现边界

- 回放包由 `cargo run --release --example export_playback` 从 Rust 的权威事件日志生成；
- 发布包为 6.5 MB 左右的紧凑 JSON，拓扑只存一次，前端只解码当前分支；
- 神经元显示采样按 10 ms 降采样，但放电时间、输入来源和在途区间保持事件时间；
- 当前验收覆盖 100 神经元冻结基线；300 神经元和目标 Mac 的帧时间属于下一轮性能复测；
- Gate 3 只读取已有分支，不从界面创建新的模拟分支，创建与保存干预仍属于 Gate 4。
