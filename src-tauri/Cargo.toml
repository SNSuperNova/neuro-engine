[package]
name = "neuro-engine-app"
version = "0.1.0"
description = "Neuro Engine Gate 3 neural microscope"
edition = "2024"

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }

[dependencies]
tauri = { version = "=2.11.5", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
